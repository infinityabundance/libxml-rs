// GENERATED by tools/abi/rust_mirror_court.py — do not hand-edit.
// Measures the Rust #[repr(C)] mirrors of the C ABI.
// Output is normalized through rustfmt at generation time so the
// committed example stays byte-identical to generator output AND
// passes `cargo fmt --check` (11.1-Z seal requirement).
use libxml_rs::abi::structs::*;
use libxml_rs::abi::types::*;
use std::alloc::{alloc_zeroed, dealloc, Layout};
use std::mem::{align_of, offset_of, size_of, size_of_val};
fn main() {
    println!(
        "STRUCT EncodingInputUnion sizeof={} alignof={}",
        size_of::<EncodingInputUnion>(),
        align_of::<EncodingInputUnion>()
    );
    {
        unsafe {
            let layout = Layout::new::<EncodingInputUnion>();
            let p = alloc_zeroed(layout) as *mut EncodingInputUnion;
            println!(
                "FIELD EncodingInputUnion.func offsetof={} size={}",
                offset_of!(EncodingInputUnion, func),
                size_of_val(&(*p).func)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<EncodingInputUnion>();
            let p = alloc_zeroed(layout) as *mut EncodingInputUnion;
            println!(
                "FIELD EncodingInputUnion.legacyFunc offsetof={} size={}",
                offset_of!(EncodingInputUnion, legacyFunc),
                size_of_val(&(*p).legacyFunc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT EncodingOutputUnion sizeof={} alignof={}",
        size_of::<EncodingOutputUnion>(),
        align_of::<EncodingOutputUnion>()
    );
    {
        unsafe {
            let layout = Layout::new::<EncodingOutputUnion>();
            let p = alloc_zeroed(layout) as *mut EncodingOutputUnion;
            println!(
                "FIELD EncodingOutputUnion.func offsetof={} size={}",
                offset_of!(EncodingOutputUnion, func),
                size_of_val(&(*p).func)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<EncodingOutputUnion>();
            let p = alloc_zeroed(layout) as *mut EncodingOutputUnion;
            println!(
                "FIELD EncodingOutputUnion.legacyFunc offsetof={} size={}",
                offset_of!(EncodingOutputUnion, legacyFunc),
                size_of_val(&(*p).legacyFunc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlAttr sizeof={} alignof={}",
        size_of::<_xmlAttr>(),
        align_of::<_xmlAttr>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr._private offsetof={} size={}",
                offset_of!(_xmlAttr, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.type_ offsetof={} size={}",
                offset_of!(_xmlAttr, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.name offsetof={} size={}",
                offset_of!(_xmlAttr, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.children offsetof={} size={}",
                offset_of!(_xmlAttr, children),
                size_of_val(&(*p).children)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.last offsetof={} size={}",
                offset_of!(_xmlAttr, last),
                size_of_val(&(*p).last)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.parent offsetof={} size={}",
                offset_of!(_xmlAttr, parent),
                size_of_val(&(*p).parent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.next offsetof={} size={}",
                offset_of!(_xmlAttr, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.prev offsetof={} size={}",
                offset_of!(_xmlAttr, prev),
                size_of_val(&(*p).prev)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.doc offsetof={} size={}",
                offset_of!(_xmlAttr, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.ns offsetof={} size={}",
                offset_of!(_xmlAttr, ns),
                size_of_val(&(*p).ns)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.atype offsetof={} size={}",
                offset_of!(_xmlAttr, atype),
                size_of_val(&(*p).atype)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.psvi offsetof={} size={}",
                offset_of!(_xmlAttr, psvi),
                size_of_val(&(*p).psvi)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttr>();
            let p = alloc_zeroed(layout) as *mut _xmlAttr;
            println!(
                "FIELD _xmlAttr.id offsetof={} size={}",
                offset_of!(_xmlAttr, id),
                size_of_val(&(*p).id)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlAttribute sizeof={} alignof={}",
        size_of::<_xmlAttribute>(),
        align_of::<_xmlAttribute>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute._private offsetof={} size={}",
                offset_of!(_xmlAttribute, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.type_ offsetof={} size={}",
                offset_of!(_xmlAttribute, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.name offsetof={} size={}",
                offset_of!(_xmlAttribute, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.children offsetof={} size={}",
                offset_of!(_xmlAttribute, children),
                size_of_val(&(*p).children)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.last offsetof={} size={}",
                offset_of!(_xmlAttribute, last),
                size_of_val(&(*p).last)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.parent offsetof={} size={}",
                offset_of!(_xmlAttribute, parent),
                size_of_val(&(*p).parent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.next offsetof={} size={}",
                offset_of!(_xmlAttribute, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.prev offsetof={} size={}",
                offset_of!(_xmlAttribute, prev),
                size_of_val(&(*p).prev)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.doc offsetof={} size={}",
                offset_of!(_xmlAttribute, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.nexth offsetof={} size={}",
                offset_of!(_xmlAttribute, nexth),
                size_of_val(&(*p).nexth)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.atype offsetof={} size={}",
                offset_of!(_xmlAttribute, atype),
                size_of_val(&(*p).atype)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.def offsetof={} size={}",
                offset_of!(_xmlAttribute, def),
                size_of_val(&(*p).def)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.defaultValue offsetof={} size={}",
                offset_of!(_xmlAttribute, defaultValue),
                size_of_val(&(*p).defaultValue)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.tree offsetof={} size={}",
                offset_of!(_xmlAttribute, tree),
                size_of_val(&(*p).tree)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.prefix offsetof={} size={}",
                offset_of!(_xmlAttribute, prefix),
                size_of_val(&(*p).prefix)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlAttribute>();
            let p = alloc_zeroed(layout) as *mut _xmlAttribute;
            println!(
                "FIELD _xmlAttribute.elem offsetof={} size={}",
                offset_of!(_xmlAttribute, elem),
                size_of_val(&(*p).elem)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlBuf sizeof={} alignof={}",
        size_of::<_xmlBuf>(),
        align_of::<_xmlBuf>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlBuf>();
            let p = alloc_zeroed(layout) as *mut _xmlBuf;
            println!(
                "FIELD _xmlBuf.content offsetof={} size={}",
                offset_of!(_xmlBuf, content),
                size_of_val(&(*p).content)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuf>();
            let p = alloc_zeroed(layout) as *mut _xmlBuf;
            println!(
                "FIELD _xmlBuf.use_ offsetof={} size={}",
                offset_of!(_xmlBuf, use_),
                size_of_val(&(*p).use_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuf>();
            let p = alloc_zeroed(layout) as *mut _xmlBuf;
            println!(
                "FIELD _xmlBuf.size offsetof={} size={}",
                offset_of!(_xmlBuf, size),
                size_of_val(&(*p).size)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuf>();
            let p = alloc_zeroed(layout) as *mut _xmlBuf;
            println!(
                "FIELD _xmlBuf.alloc offsetof={} size={}",
                offset_of!(_xmlBuf, alloc),
                size_of_val(&(*p).alloc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuf>();
            let p = alloc_zeroed(layout) as *mut _xmlBuf;
            println!(
                "FIELD _xmlBuf.error offsetof={} size={}",
                offset_of!(_xmlBuf, error),
                size_of_val(&(*p).error)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuf>();
            let p = alloc_zeroed(layout) as *mut _xmlBuf;
            println!(
                "FIELD _xmlBuf.buffer offsetof={} size={}",
                offset_of!(_xmlBuf, buffer),
                size_of_val(&(*p).buffer)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuf>();
            let p = alloc_zeroed(layout) as *mut _xmlBuf;
            println!(
                "FIELD _xmlBuf.io offsetof={} size={}",
                offset_of!(_xmlBuf, io),
                size_of_val(&(*p).io)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlBuffer sizeof={} alignof={}",
        size_of::<_xmlBuffer>(),
        align_of::<_xmlBuffer>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlBuffer;
            println!(
                "FIELD _xmlBuffer.content offsetof={} size={}",
                offset_of!(_xmlBuffer, content),
                size_of_val(&(*p).content)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlBuffer;
            println!(
                "FIELD _xmlBuffer.use_ offsetof={} size={}",
                offset_of!(_xmlBuffer, use_),
                size_of_val(&(*p).use_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlBuffer;
            println!(
                "FIELD _xmlBuffer.size offsetof={} size={}",
                offset_of!(_xmlBuffer, size),
                size_of_val(&(*p).size)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlBuffer;
            println!(
                "FIELD _xmlBuffer.alloc offsetof={} size={}",
                offset_of!(_xmlBuffer, alloc),
                size_of_val(&(*p).alloc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlBuffer;
            println!(
                "FIELD _xmlBuffer.contentIO offsetof={} size={}",
                offset_of!(_xmlBuffer, contentIO),
                size_of_val(&(*p).contentIO)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlCharEncodingHandler sizeof={} alignof={}",
        size_of::<_xmlCharEncodingHandler>(),
        align_of::<_xmlCharEncodingHandler>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlCharEncodingHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlCharEncodingHandler;
            println!(
                "FIELD _xmlCharEncodingHandler.name offsetof={} size={}",
                offset_of!(_xmlCharEncodingHandler, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlCharEncodingHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlCharEncodingHandler;
            println!(
                "FIELD _xmlCharEncodingHandler.input offsetof={} size={}",
                offset_of!(_xmlCharEncodingHandler, input),
                size_of_val(&(*p).input)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlCharEncodingHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlCharEncodingHandler;
            println!(
                "FIELD _xmlCharEncodingHandler.output offsetof={} size={}",
                offset_of!(_xmlCharEncodingHandler, output),
                size_of_val(&(*p).output)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlCharEncodingHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlCharEncodingHandler;
            println!(
                "FIELD _xmlCharEncodingHandler.inputCtxt offsetof={} size={}",
                offset_of!(_xmlCharEncodingHandler, inputCtxt),
                size_of_val(&(*p).inputCtxt)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlCharEncodingHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlCharEncodingHandler;
            println!(
                "FIELD _xmlCharEncodingHandler.outputCtxt offsetof={} size={}",
                offset_of!(_xmlCharEncodingHandler, outputCtxt),
                size_of_val(&(*p).outputCtxt)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlCharEncodingHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlCharEncodingHandler;
            println!(
                "FIELD _xmlCharEncodingHandler.ctxtDtor offsetof={} size={}",
                offset_of!(_xmlCharEncodingHandler, ctxtDtor),
                size_of_val(&(*p).ctxtDtor)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlCharEncodingHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlCharEncodingHandler;
            println!(
                "FIELD _xmlCharEncodingHandler.flags offsetof={} size={}",
                offset_of!(_xmlCharEncodingHandler, flags),
                size_of_val(&(*p).flags)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlDoc sizeof={} alignof={}",
        size_of::<_xmlDoc>(),
        align_of::<_xmlDoc>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc._private offsetof={} size={}",
                offset_of!(_xmlDoc, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.type_ offsetof={} size={}",
                offset_of!(_xmlDoc, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.name offsetof={} size={}",
                offset_of!(_xmlDoc, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.children offsetof={} size={}",
                offset_of!(_xmlDoc, children),
                size_of_val(&(*p).children)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.last offsetof={} size={}",
                offset_of!(_xmlDoc, last),
                size_of_val(&(*p).last)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.parent offsetof={} size={}",
                offset_of!(_xmlDoc, parent),
                size_of_val(&(*p).parent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.next offsetof={} size={}",
                offset_of!(_xmlDoc, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.prev offsetof={} size={}",
                offset_of!(_xmlDoc, prev),
                size_of_val(&(*p).prev)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.doc offsetof={} size={}",
                offset_of!(_xmlDoc, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.compression offsetof={} size={}",
                offset_of!(_xmlDoc, compression),
                size_of_val(&(*p).compression)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.standalone offsetof={} size={}",
                offset_of!(_xmlDoc, standalone),
                size_of_val(&(*p).standalone)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.intSubset offsetof={} size={}",
                offset_of!(_xmlDoc, intSubset),
                size_of_val(&(*p).intSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.extSubset offsetof={} size={}",
                offset_of!(_xmlDoc, extSubset),
                size_of_val(&(*p).extSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.oldNs offsetof={} size={}",
                offset_of!(_xmlDoc, oldNs),
                size_of_val(&(*p).oldNs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.version offsetof={} size={}",
                offset_of!(_xmlDoc, version),
                size_of_val(&(*p).version)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.encoding offsetof={} size={}",
                offset_of!(_xmlDoc, encoding),
                size_of_val(&(*p).encoding)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.ids offsetof={} size={}",
                offset_of!(_xmlDoc, ids),
                size_of_val(&(*p).ids)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.refs offsetof={} size={}",
                offset_of!(_xmlDoc, refs),
                size_of_val(&(*p).refs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.URL offsetof={} size={}",
                offset_of!(_xmlDoc, URL),
                size_of_val(&(*p).URL)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.charset offsetof={} size={}",
                offset_of!(_xmlDoc, charset),
                size_of_val(&(*p).charset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.dict offsetof={} size={}",
                offset_of!(_xmlDoc, dict),
                size_of_val(&(*p).dict)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.psvi offsetof={} size={}",
                offset_of!(_xmlDoc, psvi),
                size_of_val(&(*p).psvi)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.parseFlags offsetof={} size={}",
                offset_of!(_xmlDoc, parseFlags),
                size_of_val(&(*p).parseFlags)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDoc>();
            let p = alloc_zeroed(layout) as *mut _xmlDoc;
            println!(
                "FIELD _xmlDoc.properties offsetof={} size={}",
                offset_of!(_xmlDoc, properties),
                size_of_val(&(*p).properties)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlDtd sizeof={} alignof={}",
        size_of::<_xmlDtd>(),
        align_of::<_xmlDtd>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd._private offsetof={} size={}",
                offset_of!(_xmlDtd, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.type_ offsetof={} size={}",
                offset_of!(_xmlDtd, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.name offsetof={} size={}",
                offset_of!(_xmlDtd, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.children offsetof={} size={}",
                offset_of!(_xmlDtd, children),
                size_of_val(&(*p).children)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.last offsetof={} size={}",
                offset_of!(_xmlDtd, last),
                size_of_val(&(*p).last)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.parent offsetof={} size={}",
                offset_of!(_xmlDtd, parent),
                size_of_val(&(*p).parent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.next offsetof={} size={}",
                offset_of!(_xmlDtd, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.prev offsetof={} size={}",
                offset_of!(_xmlDtd, prev),
                size_of_val(&(*p).prev)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.doc offsetof={} size={}",
                offset_of!(_xmlDtd, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.notations offsetof={} size={}",
                offset_of!(_xmlDtd, notations),
                size_of_val(&(*p).notations)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.elements offsetof={} size={}",
                offset_of!(_xmlDtd, elements),
                size_of_val(&(*p).elements)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.attributes offsetof={} size={}",
                offset_of!(_xmlDtd, attributes),
                size_of_val(&(*p).attributes)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.entities offsetof={} size={}",
                offset_of!(_xmlDtd, entities),
                size_of_val(&(*p).entities)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.ExternalID offsetof={} size={}",
                offset_of!(_xmlDtd, ExternalID),
                size_of_val(&(*p).ExternalID)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.SystemID offsetof={} size={}",
                offset_of!(_xmlDtd, SystemID),
                size_of_val(&(*p).SystemID)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlDtd>();
            let p = alloc_zeroed(layout) as *mut _xmlDtd;
            println!(
                "FIELD _xmlDtd.pentities offsetof={} size={}",
                offset_of!(_xmlDtd, pentities),
                size_of_val(&(*p).pentities)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlElement sizeof={} alignof={}",
        size_of::<_xmlElement>(),
        align_of::<_xmlElement>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement._private offsetof={} size={}",
                offset_of!(_xmlElement, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.type_ offsetof={} size={}",
                offset_of!(_xmlElement, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.name offsetof={} size={}",
                offset_of!(_xmlElement, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.children offsetof={} size={}",
                offset_of!(_xmlElement, children),
                size_of_val(&(*p).children)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.last offsetof={} size={}",
                offset_of!(_xmlElement, last),
                size_of_val(&(*p).last)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.parent offsetof={} size={}",
                offset_of!(_xmlElement, parent),
                size_of_val(&(*p).parent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.next offsetof={} size={}",
                offset_of!(_xmlElement, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.prev offsetof={} size={}",
                offset_of!(_xmlElement, prev),
                size_of_val(&(*p).prev)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.doc offsetof={} size={}",
                offset_of!(_xmlElement, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.etype offsetof={} size={}",
                offset_of!(_xmlElement, etype),
                size_of_val(&(*p).etype)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.content offsetof={} size={}",
                offset_of!(_xmlElement, content),
                size_of_val(&(*p).content)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.attributes offsetof={} size={}",
                offset_of!(_xmlElement, attributes),
                size_of_val(&(*p).attributes)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.prefix offsetof={} size={}",
                offset_of!(_xmlElement, prefix),
                size_of_val(&(*p).prefix)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElement>();
            let p = alloc_zeroed(layout) as *mut _xmlElement;
            println!(
                "FIELD _xmlElement.cont_model offsetof={} size={}",
                offset_of!(_xmlElement, cont_model),
                size_of_val(&(*p).cont_model)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlElementContent sizeof={} alignof={}",
        size_of::<_xmlElementContent>(),
        align_of::<_xmlElementContent>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlElementContent>();
            let p = alloc_zeroed(layout) as *mut _xmlElementContent;
            println!(
                "FIELD _xmlElementContent.type_ offsetof={} size={}",
                offset_of!(_xmlElementContent, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElementContent>();
            let p = alloc_zeroed(layout) as *mut _xmlElementContent;
            println!(
                "FIELD _xmlElementContent.ocur offsetof={} size={}",
                offset_of!(_xmlElementContent, ocur),
                size_of_val(&(*p).ocur)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElementContent>();
            let p = alloc_zeroed(layout) as *mut _xmlElementContent;
            println!(
                "FIELD _xmlElementContent.name offsetof={} size={}",
                offset_of!(_xmlElementContent, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElementContent>();
            let p = alloc_zeroed(layout) as *mut _xmlElementContent;
            println!(
                "FIELD _xmlElementContent.c1 offsetof={} size={}",
                offset_of!(_xmlElementContent, c1),
                size_of_val(&(*p).c1)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElementContent>();
            let p = alloc_zeroed(layout) as *mut _xmlElementContent;
            println!(
                "FIELD _xmlElementContent.c2 offsetof={} size={}",
                offset_of!(_xmlElementContent, c2),
                size_of_val(&(*p).c2)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElementContent>();
            let p = alloc_zeroed(layout) as *mut _xmlElementContent;
            println!(
                "FIELD _xmlElementContent.parent offsetof={} size={}",
                offset_of!(_xmlElementContent, parent),
                size_of_val(&(*p).parent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlElementContent>();
            let p = alloc_zeroed(layout) as *mut _xmlElementContent;
            println!(
                "FIELD _xmlElementContent.prefix offsetof={} size={}",
                offset_of!(_xmlElementContent, prefix),
                size_of_val(&(*p).prefix)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlEntity sizeof={} alignof={}",
        size_of::<_xmlEntity>(),
        align_of::<_xmlEntity>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity._private offsetof={} size={}",
                offset_of!(_xmlEntity, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.type_ offsetof={} size={}",
                offset_of!(_xmlEntity, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.name offsetof={} size={}",
                offset_of!(_xmlEntity, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.children offsetof={} size={}",
                offset_of!(_xmlEntity, children),
                size_of_val(&(*p).children)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.last offsetof={} size={}",
                offset_of!(_xmlEntity, last),
                size_of_val(&(*p).last)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.parent offsetof={} size={}",
                offset_of!(_xmlEntity, parent),
                size_of_val(&(*p).parent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.next offsetof={} size={}",
                offset_of!(_xmlEntity, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.prev offsetof={} size={}",
                offset_of!(_xmlEntity, prev),
                size_of_val(&(*p).prev)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.doc offsetof={} size={}",
                offset_of!(_xmlEntity, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.orig offsetof={} size={}",
                offset_of!(_xmlEntity, orig),
                size_of_val(&(*p).orig)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.content offsetof={} size={}",
                offset_of!(_xmlEntity, content),
                size_of_val(&(*p).content)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.length offsetof={} size={}",
                offset_of!(_xmlEntity, length),
                size_of_val(&(*p).length)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.etype offsetof={} size={}",
                offset_of!(_xmlEntity, etype),
                size_of_val(&(*p).etype)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.ExternalID offsetof={} size={}",
                offset_of!(_xmlEntity, ExternalID),
                size_of_val(&(*p).ExternalID)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.SystemID offsetof={} size={}",
                offset_of!(_xmlEntity, SystemID),
                size_of_val(&(*p).SystemID)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.nexte offsetof={} size={}",
                offset_of!(_xmlEntity, nexte),
                size_of_val(&(*p).nexte)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.URI offsetof={} size={}",
                offset_of!(_xmlEntity, URI),
                size_of_val(&(*p).URI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.owner offsetof={} size={}",
                offset_of!(_xmlEntity, owner),
                size_of_val(&(*p).owner)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.flags offsetof={} size={}",
                offset_of!(_xmlEntity, flags),
                size_of_val(&(*p).flags)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEntity>();
            let p = alloc_zeroed(layout) as *mut _xmlEntity;
            println!(
                "FIELD _xmlEntity.expandedSize offsetof={} size={}",
                offset_of!(_xmlEntity, expandedSize),
                size_of_val(&(*p).expandedSize)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlEnumeration sizeof={} alignof={}",
        size_of::<_xmlEnumeration>(),
        align_of::<_xmlEnumeration>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlEnumeration>();
            let p = alloc_zeroed(layout) as *mut _xmlEnumeration;
            println!(
                "FIELD _xmlEnumeration.next offsetof={} size={}",
                offset_of!(_xmlEnumeration, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlEnumeration>();
            let p = alloc_zeroed(layout) as *mut _xmlEnumeration;
            println!(
                "FIELD _xmlEnumeration.name offsetof={} size={}",
                offset_of!(_xmlEnumeration, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlError sizeof={} alignof={}",
        size_of::<_xmlError>(),
        align_of::<_xmlError>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.domain offsetof={} size={}",
                offset_of!(_xmlError, domain),
                size_of_val(&(*p).domain)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.code offsetof={} size={}",
                offset_of!(_xmlError, code),
                size_of_val(&(*p).code)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.message offsetof={} size={}",
                offset_of!(_xmlError, message),
                size_of_val(&(*p).message)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.level offsetof={} size={}",
                offset_of!(_xmlError, level),
                size_of_val(&(*p).level)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.file offsetof={} size={}",
                offset_of!(_xmlError, file),
                size_of_val(&(*p).file)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.line offsetof={} size={}",
                offset_of!(_xmlError, line),
                size_of_val(&(*p).line)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.str1 offsetof={} size={}",
                offset_of!(_xmlError, str1),
                size_of_val(&(*p).str1)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.str2 offsetof={} size={}",
                offset_of!(_xmlError, str2),
                size_of_val(&(*p).str2)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.str3 offsetof={} size={}",
                offset_of!(_xmlError, str3),
                size_of_val(&(*p).str3)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.int1 offsetof={} size={}",
                offset_of!(_xmlError, int1),
                size_of_val(&(*p).int1)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.int2 offsetof={} size={}",
                offset_of!(_xmlError, int2),
                size_of_val(&(*p).int2)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.ctxt offsetof={} size={}",
                offset_of!(_xmlError, ctxt),
                size_of_val(&(*p).ctxt)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlError>();
            let p = alloc_zeroed(layout) as *mut _xmlError;
            println!(
                "FIELD _xmlError.node offsetof={} size={}",
                offset_of!(_xmlError, node),
                size_of_val(&(*p).node)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlID sizeof={} alignof={}",
        size_of::<_xmlID>(),
        align_of::<_xmlID>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlID>();
            let p = alloc_zeroed(layout) as *mut _xmlID;
            println!(
                "FIELD _xmlID.next offsetof={} size={}",
                offset_of!(_xmlID, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlID>();
            let p = alloc_zeroed(layout) as *mut _xmlID;
            println!(
                "FIELD _xmlID.value offsetof={} size={}",
                offset_of!(_xmlID, value),
                size_of_val(&(*p).value)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlID>();
            let p = alloc_zeroed(layout) as *mut _xmlID;
            println!(
                "FIELD _xmlID.attr offsetof={} size={}",
                offset_of!(_xmlID, attr),
                size_of_val(&(*p).attr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlID>();
            let p = alloc_zeroed(layout) as *mut _xmlID;
            println!(
                "FIELD _xmlID.name offsetof={} size={}",
                offset_of!(_xmlID, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlID>();
            let p = alloc_zeroed(layout) as *mut _xmlID;
            println!(
                "FIELD _xmlID.lineno offsetof={} size={}",
                offset_of!(_xmlID, lineno),
                size_of_val(&(*p).lineno)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlID>();
            let p = alloc_zeroed(layout) as *mut _xmlID;
            println!(
                "FIELD _xmlID.doc offsetof={} size={}",
                offset_of!(_xmlID, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlNode sizeof={} alignof={}",
        size_of::<_xmlNode>(),
        align_of::<_xmlNode>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode._private offsetof={} size={}",
                offset_of!(_xmlNode, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.type_ offsetof={} size={}",
                offset_of!(_xmlNode, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.name offsetof={} size={}",
                offset_of!(_xmlNode, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.children offsetof={} size={}",
                offset_of!(_xmlNode, children),
                size_of_val(&(*p).children)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.last offsetof={} size={}",
                offset_of!(_xmlNode, last),
                size_of_val(&(*p).last)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.parent offsetof={} size={}",
                offset_of!(_xmlNode, parent),
                size_of_val(&(*p).parent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.next offsetof={} size={}",
                offset_of!(_xmlNode, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.prev offsetof={} size={}",
                offset_of!(_xmlNode, prev),
                size_of_val(&(*p).prev)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.doc offsetof={} size={}",
                offset_of!(_xmlNode, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.ns offsetof={} size={}",
                offset_of!(_xmlNode, ns),
                size_of_val(&(*p).ns)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.content offsetof={} size={}",
                offset_of!(_xmlNode, content),
                size_of_val(&(*p).content)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.properties offsetof={} size={}",
                offset_of!(_xmlNode, properties),
                size_of_val(&(*p).properties)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.nsDef offsetof={} size={}",
                offset_of!(_xmlNode, nsDef),
                size_of_val(&(*p).nsDef)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.psvi offsetof={} size={}",
                offset_of!(_xmlNode, psvi),
                size_of_val(&(*p).psvi)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.line offsetof={} size={}",
                offset_of!(_xmlNode, line),
                size_of_val(&(*p).line)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNode>();
            let p = alloc_zeroed(layout) as *mut _xmlNode;
            println!(
                "FIELD _xmlNode.extra offsetof={} size={}",
                offset_of!(_xmlNode, extra),
                size_of_val(&(*p).extra)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlNodeSet sizeof={} alignof={}",
        size_of::<_xmlNodeSet>(),
        align_of::<_xmlNodeSet>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlNodeSet>();
            let p = alloc_zeroed(layout) as *mut _xmlNodeSet;
            println!(
                "FIELD _xmlNodeSet.nodeNr offsetof={} size={}",
                offset_of!(_xmlNodeSet, nodeNr),
                size_of_val(&(*p).nodeNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNodeSet>();
            let p = alloc_zeroed(layout) as *mut _xmlNodeSet;
            println!(
                "FIELD _xmlNodeSet.nodeMax offsetof={} size={}",
                offset_of!(_xmlNodeSet, nodeMax),
                size_of_val(&(*p).nodeMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNodeSet>();
            let p = alloc_zeroed(layout) as *mut _xmlNodeSet;
            println!(
                "FIELD _xmlNodeSet.nodeTab offsetof={} size={}",
                offset_of!(_xmlNodeSet, nodeTab),
                size_of_val(&(*p).nodeTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlNotation sizeof={} alignof={}",
        size_of::<_xmlNotation>(),
        align_of::<_xmlNotation>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlNotation>();
            let p = alloc_zeroed(layout) as *mut _xmlNotation;
            println!(
                "FIELD _xmlNotation.name offsetof={} size={}",
                offset_of!(_xmlNotation, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNotation>();
            let p = alloc_zeroed(layout) as *mut _xmlNotation;
            println!(
                "FIELD _xmlNotation.PublicID offsetof={} size={}",
                offset_of!(_xmlNotation, PublicID),
                size_of_val(&(*p).PublicID)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNotation>();
            let p = alloc_zeroed(layout) as *mut _xmlNotation;
            println!(
                "FIELD _xmlNotation.SystemID offsetof={} size={}",
                offset_of!(_xmlNotation, SystemID),
                size_of_val(&(*p).SystemID)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlNs sizeof={} alignof={}",
        size_of::<_xmlNs>(),
        align_of::<_xmlNs>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlNs>();
            let p = alloc_zeroed(layout) as *mut _xmlNs;
            println!(
                "FIELD _xmlNs.next offsetof={} size={}",
                offset_of!(_xmlNs, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNs>();
            let p = alloc_zeroed(layout) as *mut _xmlNs;
            println!(
                "FIELD _xmlNs.type_ offsetof={} size={}",
                offset_of!(_xmlNs, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNs>();
            let p = alloc_zeroed(layout) as *mut _xmlNs;
            println!(
                "FIELD _xmlNs.href offsetof={} size={}",
                offset_of!(_xmlNs, href),
                size_of_val(&(*p).href)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNs>();
            let p = alloc_zeroed(layout) as *mut _xmlNs;
            println!(
                "FIELD _xmlNs.prefix offsetof={} size={}",
                offset_of!(_xmlNs, prefix),
                size_of_val(&(*p).prefix)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNs>();
            let p = alloc_zeroed(layout) as *mut _xmlNs;
            println!(
                "FIELD _xmlNs._private offsetof={} size={}",
                offset_of!(_xmlNs, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlNs>();
            let p = alloc_zeroed(layout) as *mut _xmlNs;
            println!(
                "FIELD _xmlNs.context offsetof={} size={}",
                offset_of!(_xmlNs, context),
                size_of_val(&(*p).context)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlOutputBuffer sizeof={} alignof={}",
        size_of::<_xmlOutputBuffer>(),
        align_of::<_xmlOutputBuffer>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlOutputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlOutputBuffer;
            println!(
                "FIELD _xmlOutputBuffer.context offsetof={} size={}",
                offset_of!(_xmlOutputBuffer, context),
                size_of_val(&(*p).context)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlOutputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlOutputBuffer;
            println!(
                "FIELD _xmlOutputBuffer.writecallback offsetof={} size={}",
                offset_of!(_xmlOutputBuffer, writecallback),
                size_of_val(&(*p).writecallback)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlOutputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlOutputBuffer;
            println!(
                "FIELD _xmlOutputBuffer.closecallback offsetof={} size={}",
                offset_of!(_xmlOutputBuffer, closecallback),
                size_of_val(&(*p).closecallback)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlOutputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlOutputBuffer;
            println!(
                "FIELD _xmlOutputBuffer.encoder offsetof={} size={}",
                offset_of!(_xmlOutputBuffer, encoder),
                size_of_val(&(*p).encoder)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlOutputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlOutputBuffer;
            println!(
                "FIELD _xmlOutputBuffer.buffer offsetof={} size={}",
                offset_of!(_xmlOutputBuffer, buffer),
                size_of_val(&(*p).buffer)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlOutputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlOutputBuffer;
            println!(
                "FIELD _xmlOutputBuffer.conv offsetof={} size={}",
                offset_of!(_xmlOutputBuffer, conv),
                size_of_val(&(*p).conv)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlOutputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlOutputBuffer;
            println!(
                "FIELD _xmlOutputBuffer.written offsetof={} size={}",
                offset_of!(_xmlOutputBuffer, written),
                size_of_val(&(*p).written)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlOutputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlOutputBuffer;
            println!(
                "FIELD _xmlOutputBuffer.error offsetof={} size={}",
                offset_of!(_xmlOutputBuffer, error),
                size_of_val(&(*p).error)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlParserCtxt sizeof={} alignof={}",
        size_of::<_xmlParserCtxt>(),
        align_of::<_xmlParserCtxt>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.sax offsetof={} size={}",
                offset_of!(_xmlParserCtxt, sax),
                size_of_val(&(*p).sax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.userData offsetof={} size={}",
                offset_of!(_xmlParserCtxt, userData),
                size_of_val(&(*p).userData)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.myDoc offsetof={} size={}",
                offset_of!(_xmlParserCtxt, myDoc),
                size_of_val(&(*p).myDoc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.wellFormed offsetof={} size={}",
                offset_of!(_xmlParserCtxt, wellFormed),
                size_of_val(&(*p).wellFormed)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.replaceEntities offsetof={} size={}",
                offset_of!(_xmlParserCtxt, replaceEntities),
                size_of_val(&(*p).replaceEntities)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.version offsetof={} size={}",
                offset_of!(_xmlParserCtxt, version),
                size_of_val(&(*p).version)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.encoding offsetof={} size={}",
                offset_of!(_xmlParserCtxt, encoding),
                size_of_val(&(*p).encoding)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.standalone offsetof={} size={}",
                offset_of!(_xmlParserCtxt, standalone),
                size_of_val(&(*p).standalone)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.html offsetof={} size={}",
                offset_of!(_xmlParserCtxt, html),
                size_of_val(&(*p).html)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.input offsetof={} size={}",
                offset_of!(_xmlParserCtxt, input),
                size_of_val(&(*p).input)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.inputNr offsetof={} size={}",
                offset_of!(_xmlParserCtxt, inputNr),
                size_of_val(&(*p).inputNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.inputMax offsetof={} size={}",
                offset_of!(_xmlParserCtxt, inputMax),
                size_of_val(&(*p).inputMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.inputTab offsetof={} size={}",
                offset_of!(_xmlParserCtxt, inputTab),
                size_of_val(&(*p).inputTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.node offsetof={} size={}",
                offset_of!(_xmlParserCtxt, node),
                size_of_val(&(*p).node)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nodeNr offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nodeNr),
                size_of_val(&(*p).nodeNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nodeMax offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nodeMax),
                size_of_val(&(*p).nodeMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nodeTab offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nodeTab),
                size_of_val(&(*p).nodeTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.record_info offsetof={} size={}",
                offset_of!(_xmlParserCtxt, record_info),
                size_of_val(&(*p).record_info)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.node_seq offsetof={} size={}",
                offset_of!(_xmlParserCtxt, node_seq),
                size_of_val(&(*p).node_seq)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.errNo offsetof={} size={}",
                offset_of!(_xmlParserCtxt, errNo),
                size_of_val(&(*p).errNo)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.hasExternalSubset offsetof={} size={}",
                offset_of!(_xmlParserCtxt, hasExternalSubset),
                size_of_val(&(*p).hasExternalSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.hasPErefs offsetof={} size={}",
                offset_of!(_xmlParserCtxt, hasPErefs),
                size_of_val(&(*p).hasPErefs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.external offsetof={} size={}",
                offset_of!(_xmlParserCtxt, external),
                size_of_val(&(*p).external)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.valid offsetof={} size={}",
                offset_of!(_xmlParserCtxt, valid),
                size_of_val(&(*p).valid)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.validate offsetof={} size={}",
                offset_of!(_xmlParserCtxt, validate),
                size_of_val(&(*p).validate)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.vctxt offsetof={} size={}",
                offset_of!(_xmlParserCtxt, vctxt),
                size_of_val(&(*p).vctxt)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.instate offsetof={} size={}",
                offset_of!(_xmlParserCtxt, instate),
                size_of_val(&(*p).instate)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.token offsetof={} size={}",
                offset_of!(_xmlParserCtxt, token),
                size_of_val(&(*p).token)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.directory offsetof={} size={}",
                offset_of!(_xmlParserCtxt, directory),
                size_of_val(&(*p).directory)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.name offsetof={} size={}",
                offset_of!(_xmlParserCtxt, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nameNr offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nameNr),
                size_of_val(&(*p).nameNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nameMax offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nameMax),
                size_of_val(&(*p).nameMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nameTab offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nameTab),
                size_of_val(&(*p).nameTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nbChars offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nbChars),
                size_of_val(&(*p).nbChars)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.checkIndex offsetof={} size={}",
                offset_of!(_xmlParserCtxt, checkIndex),
                size_of_val(&(*p).checkIndex)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.keepBlanks offsetof={} size={}",
                offset_of!(_xmlParserCtxt, keepBlanks),
                size_of_val(&(*p).keepBlanks)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.disableSAX offsetof={} size={}",
                offset_of!(_xmlParserCtxt, disableSAX),
                size_of_val(&(*p).disableSAX)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.inSubset offsetof={} size={}",
                offset_of!(_xmlParserCtxt, inSubset),
                size_of_val(&(*p).inSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.intSubName offsetof={} size={}",
                offset_of!(_xmlParserCtxt, intSubName),
                size_of_val(&(*p).intSubName)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.extSubURI offsetof={} size={}",
                offset_of!(_xmlParserCtxt, extSubURI),
                size_of_val(&(*p).extSubURI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.extSubSystem offsetof={} size={}",
                offset_of!(_xmlParserCtxt, extSubSystem),
                size_of_val(&(*p).extSubSystem)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.space offsetof={} size={}",
                offset_of!(_xmlParserCtxt, space),
                size_of_val(&(*p).space)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.spaceNr offsetof={} size={}",
                offset_of!(_xmlParserCtxt, spaceNr),
                size_of_val(&(*p).spaceNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.spaceMax offsetof={} size={}",
                offset_of!(_xmlParserCtxt, spaceMax),
                size_of_val(&(*p).spaceMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.spaceTab offsetof={} size={}",
                offset_of!(_xmlParserCtxt, spaceTab),
                size_of_val(&(*p).spaceTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.depth offsetof={} size={}",
                offset_of!(_xmlParserCtxt, depth),
                size_of_val(&(*p).depth)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.entity offsetof={} size={}",
                offset_of!(_xmlParserCtxt, entity),
                size_of_val(&(*p).entity)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.charset offsetof={} size={}",
                offset_of!(_xmlParserCtxt, charset),
                size_of_val(&(*p).charset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nodelen offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nodelen),
                size_of_val(&(*p).nodelen)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nodemem offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nodemem),
                size_of_val(&(*p).nodemem)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.pedantic offsetof={} size={}",
                offset_of!(_xmlParserCtxt, pedantic),
                size_of_val(&(*p).pedantic)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt._private offsetof={} size={}",
                offset_of!(_xmlParserCtxt, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.loadsubset offsetof={} size={}",
                offset_of!(_xmlParserCtxt, loadsubset),
                size_of_val(&(*p).loadsubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.linenumbers offsetof={} size={}",
                offset_of!(_xmlParserCtxt, linenumbers),
                size_of_val(&(*p).linenumbers)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.catalogs offsetof={} size={}",
                offset_of!(_xmlParserCtxt, catalogs),
                size_of_val(&(*p).catalogs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.recovery offsetof={} size={}",
                offset_of!(_xmlParserCtxt, recovery),
                size_of_val(&(*p).recovery)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.progressive offsetof={} size={}",
                offset_of!(_xmlParserCtxt, progressive),
                size_of_val(&(*p).progressive)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.dict offsetof={} size={}",
                offset_of!(_xmlParserCtxt, dict),
                size_of_val(&(*p).dict)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.atts offsetof={} size={}",
                offset_of!(_xmlParserCtxt, atts),
                size_of_val(&(*p).atts)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.maxatts offsetof={} size={}",
                offset_of!(_xmlParserCtxt, maxatts),
                size_of_val(&(*p).maxatts)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.docdict offsetof={} size={}",
                offset_of!(_xmlParserCtxt, docdict),
                size_of_val(&(*p).docdict)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.str_xml offsetof={} size={}",
                offset_of!(_xmlParserCtxt, str_xml),
                size_of_val(&(*p).str_xml)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.str_xmlns offsetof={} size={}",
                offset_of!(_xmlParserCtxt, str_xmlns),
                size_of_val(&(*p).str_xmlns)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.str_xml_ns offsetof={} size={}",
                offset_of!(_xmlParserCtxt, str_xml_ns),
                size_of_val(&(*p).str_xml_ns)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.sax2 offsetof={} size={}",
                offset_of!(_xmlParserCtxt, sax2),
                size_of_val(&(*p).sax2)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nsNr offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nsNr),
                size_of_val(&(*p).nsNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nsMax offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nsMax),
                size_of_val(&(*p).nsMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nsTab offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nsTab),
                size_of_val(&(*p).nsTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.attallocs offsetof={} size={}",
                offset_of!(_xmlParserCtxt, attallocs),
                size_of_val(&(*p).attallocs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.pushTab offsetof={} size={}",
                offset_of!(_xmlParserCtxt, pushTab),
                size_of_val(&(*p).pushTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.attsDefault offsetof={} size={}",
                offset_of!(_xmlParserCtxt, attsDefault),
                size_of_val(&(*p).attsDefault)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.attsSpecial offsetof={} size={}",
                offset_of!(_xmlParserCtxt, attsSpecial),
                size_of_val(&(*p).attsSpecial)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nsWellFormed offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nsWellFormed),
                size_of_val(&(*p).nsWellFormed)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.options offsetof={} size={}",
                offset_of!(_xmlParserCtxt, options),
                size_of_val(&(*p).options)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.dictNames offsetof={} size={}",
                offset_of!(_xmlParserCtxt, dictNames),
                size_of_val(&(*p).dictNames)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.freeElemsNr offsetof={} size={}",
                offset_of!(_xmlParserCtxt, freeElemsNr),
                size_of_val(&(*p).freeElemsNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.freeElems offsetof={} size={}",
                offset_of!(_xmlParserCtxt, freeElems),
                size_of_val(&(*p).freeElems)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.freeAttrsNr offsetof={} size={}",
                offset_of!(_xmlParserCtxt, freeAttrsNr),
                size_of_val(&(*p).freeAttrsNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.freeAttrs offsetof={} size={}",
                offset_of!(_xmlParserCtxt, freeAttrs),
                size_of_val(&(*p).freeAttrs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.lastError offsetof={} size={}",
                offset_of!(_xmlParserCtxt, lastError),
                size_of_val(&(*p).lastError)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.parseMode offsetof={} size={}",
                offset_of!(_xmlParserCtxt, parseMode),
                size_of_val(&(*p).parseMode)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nbentities offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nbentities),
                size_of_val(&(*p).nbentities)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.sizeentities offsetof={} size={}",
                offset_of!(_xmlParserCtxt, sizeentities),
                size_of_val(&(*p).sizeentities)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nodeInfo offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nodeInfo),
                size_of_val(&(*p).nodeInfo)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nodeInfoNr offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nodeInfoNr),
                size_of_val(&(*p).nodeInfoNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nodeInfoMax offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nodeInfoMax),
                size_of_val(&(*p).nodeInfoMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nodeInfoTab offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nodeInfoTab),
                size_of_val(&(*p).nodeInfoTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.input_id offsetof={} size={}",
                offset_of!(_xmlParserCtxt, input_id),
                size_of_val(&(*p).input_id)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.sizeentcopy offsetof={} size={}",
                offset_of!(_xmlParserCtxt, sizeentcopy),
                size_of_val(&(*p).sizeentcopy)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.endCheckState offsetof={} size={}",
                offset_of!(_xmlParserCtxt, endCheckState),
                size_of_val(&(*p).endCheckState)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nbErrors offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nbErrors),
                size_of_val(&(*p).nbErrors)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nbWarnings offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nbWarnings),
                size_of_val(&(*p).nbWarnings)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.maxAmpl offsetof={} size={}",
                offset_of!(_xmlParserCtxt, maxAmpl),
                size_of_val(&(*p).maxAmpl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.nsdb offsetof={} size={}",
                offset_of!(_xmlParserCtxt, nsdb),
                size_of_val(&(*p).nsdb)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.attrHashMax offsetof={} size={}",
                offset_of!(_xmlParserCtxt, attrHashMax),
                size_of_val(&(*p).attrHashMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.attrHash offsetof={} size={}",
                offset_of!(_xmlParserCtxt, attrHash),
                size_of_val(&(*p).attrHash)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.errorHandler offsetof={} size={}",
                offset_of!(_xmlParserCtxt, errorHandler),
                size_of_val(&(*p).errorHandler)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.errorCtxt offsetof={} size={}",
                offset_of!(_xmlParserCtxt, errorCtxt),
                size_of_val(&(*p).errorCtxt)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.resourceLoader offsetof={} size={}",
                offset_of!(_xmlParserCtxt, resourceLoader),
                size_of_val(&(*p).resourceLoader)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.resourceCtxt offsetof={} size={}",
                offset_of!(_xmlParserCtxt, resourceCtxt),
                size_of_val(&(*p).resourceCtxt)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.convImpl offsetof={} size={}",
                offset_of!(_xmlParserCtxt, convImpl),
                size_of_val(&(*p).convImpl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlParserCtxt;
            println!(
                "FIELD _xmlParserCtxt.convCtxt offsetof={} size={}",
                offset_of!(_xmlParserCtxt, convCtxt),
                size_of_val(&(*p).convCtxt)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlParserInput sizeof={} alignof={}",
        size_of::<_xmlParserInput>(),
        align_of::<_xmlParserInput>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.buf offsetof={} size={}",
                offset_of!(_xmlParserInput, buf),
                size_of_val(&(*p).buf)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.filename offsetof={} size={}",
                offset_of!(_xmlParserInput, filename),
                size_of_val(&(*p).filename)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.directory offsetof={} size={}",
                offset_of!(_xmlParserInput, directory),
                size_of_val(&(*p).directory)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.base offsetof={} size={}",
                offset_of!(_xmlParserInput, base),
                size_of_val(&(*p).base)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.cur offsetof={} size={}",
                offset_of!(_xmlParserInput, cur),
                size_of_val(&(*p).cur)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.end offsetof={} size={}",
                offset_of!(_xmlParserInput, end),
                size_of_val(&(*p).end)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.length offsetof={} size={}",
                offset_of!(_xmlParserInput, length),
                size_of_val(&(*p).length)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.line offsetof={} size={}",
                offset_of!(_xmlParserInput, line),
                size_of_val(&(*p).line)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.col offsetof={} size={}",
                offset_of!(_xmlParserInput, col),
                size_of_val(&(*p).col)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.consumed offsetof={} size={}",
                offset_of!(_xmlParserInput, consumed),
                size_of_val(&(*p).consumed)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.free offsetof={} size={}",
                offset_of!(_xmlParserInput, free),
                size_of_val(&(*p).free)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.encoding offsetof={} size={}",
                offset_of!(_xmlParserInput, encoding),
                size_of_val(&(*p).encoding)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.version offsetof={} size={}",
                offset_of!(_xmlParserInput, version),
                size_of_val(&(*p).version)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.flags offsetof={} size={}",
                offset_of!(_xmlParserInput, flags),
                size_of_val(&(*p).flags)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.id offsetof={} size={}",
                offset_of!(_xmlParserInput, id),
                size_of_val(&(*p).id)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.parentConsumed offsetof={} size={}",
                offset_of!(_xmlParserInput, parentConsumed),
                size_of_val(&(*p).parentConsumed)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInput>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInput;
            println!(
                "FIELD _xmlParserInput.entity offsetof={} size={}",
                offset_of!(_xmlParserInput, entity),
                size_of_val(&(*p).entity)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlParserInputBuffer sizeof={} alignof={}",
        size_of::<_xmlParserInputBuffer>(),
        align_of::<_xmlParserInputBuffer>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInputBuffer;
            println!(
                "FIELD _xmlParserInputBuffer.context offsetof={} size={}",
                offset_of!(_xmlParserInputBuffer, context),
                size_of_val(&(*p).context)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInputBuffer;
            println!(
                "FIELD _xmlParserInputBuffer.readcallback offsetof={} size={}",
                offset_of!(_xmlParserInputBuffer, readcallback),
                size_of_val(&(*p).readcallback)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInputBuffer;
            println!(
                "FIELD _xmlParserInputBuffer.closecallback offsetof={} size={}",
                offset_of!(_xmlParserInputBuffer, closecallback),
                size_of_val(&(*p).closecallback)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInputBuffer;
            println!(
                "FIELD _xmlParserInputBuffer.encoder offsetof={} size={}",
                offset_of!(_xmlParserInputBuffer, encoder),
                size_of_val(&(*p).encoder)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInputBuffer;
            println!(
                "FIELD _xmlParserInputBuffer.buffer offsetof={} size={}",
                offset_of!(_xmlParserInputBuffer, buffer),
                size_of_val(&(*p).buffer)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInputBuffer;
            println!(
                "FIELD _xmlParserInputBuffer.raw offsetof={} size={}",
                offset_of!(_xmlParserInputBuffer, raw),
                size_of_val(&(*p).raw)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInputBuffer;
            println!(
                "FIELD _xmlParserInputBuffer.compressed offsetof={} size={}",
                offset_of!(_xmlParserInputBuffer, compressed),
                size_of_val(&(*p).compressed)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInputBuffer;
            println!(
                "FIELD _xmlParserInputBuffer.error offsetof={} size={}",
                offset_of!(_xmlParserInputBuffer, error),
                size_of_val(&(*p).error)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserInputBuffer>();
            let p = alloc_zeroed(layout) as *mut _xmlParserInputBuffer;
            println!(
                "FIELD _xmlParserInputBuffer.rawconsumed offsetof={} size={}",
                offset_of!(_xmlParserInputBuffer, rawconsumed),
                size_of_val(&(*p).rawconsumed)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlParserNodeInfo sizeof={} alignof={}",
        size_of::<_xmlParserNodeInfo>(),
        align_of::<_xmlParserNodeInfo>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlParserNodeInfo>();
            let p = alloc_zeroed(layout) as *mut _xmlParserNodeInfo;
            println!(
                "FIELD _xmlParserNodeInfo.node offsetof={} size={}",
                offset_of!(_xmlParserNodeInfo, node),
                size_of_val(&(*p).node)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserNodeInfo>();
            let p = alloc_zeroed(layout) as *mut _xmlParserNodeInfo;
            println!(
                "FIELD _xmlParserNodeInfo.begin_pos offsetof={} size={}",
                offset_of!(_xmlParserNodeInfo, begin_pos),
                size_of_val(&(*p).begin_pos)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserNodeInfo>();
            let p = alloc_zeroed(layout) as *mut _xmlParserNodeInfo;
            println!(
                "FIELD _xmlParserNodeInfo.begin_line offsetof={} size={}",
                offset_of!(_xmlParserNodeInfo, begin_line),
                size_of_val(&(*p).begin_line)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserNodeInfo>();
            let p = alloc_zeroed(layout) as *mut _xmlParserNodeInfo;
            println!(
                "FIELD _xmlParserNodeInfo.end_pos offsetof={} size={}",
                offset_of!(_xmlParserNodeInfo, end_pos),
                size_of_val(&(*p).end_pos)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserNodeInfo>();
            let p = alloc_zeroed(layout) as *mut _xmlParserNodeInfo;
            println!(
                "FIELD _xmlParserNodeInfo.end_line offsetof={} size={}",
                offset_of!(_xmlParserNodeInfo, end_line),
                size_of_val(&(*p).end_line)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlParserNodeInfoSeq sizeof={} alignof={}",
        size_of::<_xmlParserNodeInfoSeq>(),
        align_of::<_xmlParserNodeInfoSeq>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlParserNodeInfoSeq>();
            let p = alloc_zeroed(layout) as *mut _xmlParserNodeInfoSeq;
            println!(
                "FIELD _xmlParserNodeInfoSeq.maximum offsetof={} size={}",
                offset_of!(_xmlParserNodeInfoSeq, maximum),
                size_of_val(&(*p).maximum)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserNodeInfoSeq>();
            let p = alloc_zeroed(layout) as *mut _xmlParserNodeInfoSeq;
            println!(
                "FIELD _xmlParserNodeInfoSeq.length offsetof={} size={}",
                offset_of!(_xmlParserNodeInfoSeq, length),
                size_of_val(&(*p).length)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlParserNodeInfoSeq>();
            let p = alloc_zeroed(layout) as *mut _xmlParserNodeInfoSeq;
            println!(
                "FIELD _xmlParserNodeInfoSeq.buffer offsetof={} size={}",
                offset_of!(_xmlParserNodeInfoSeq, buffer),
                size_of_val(&(*p).buffer)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlRef sizeof={} alignof={}",
        size_of::<_xmlRef>(),
        align_of::<_xmlRef>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlRef>();
            let p = alloc_zeroed(layout) as *mut _xmlRef;
            println!(
                "FIELD _xmlRef.next offsetof={} size={}",
                offset_of!(_xmlRef, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlRef>();
            let p = alloc_zeroed(layout) as *mut _xmlRef;
            println!(
                "FIELD _xmlRef.value offsetof={} size={}",
                offset_of!(_xmlRef, value),
                size_of_val(&(*p).value)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlRef>();
            let p = alloc_zeroed(layout) as *mut _xmlRef;
            println!(
                "FIELD _xmlRef.attr offsetof={} size={}",
                offset_of!(_xmlRef, attr),
                size_of_val(&(*p).attr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlRef>();
            let p = alloc_zeroed(layout) as *mut _xmlRef;
            println!(
                "FIELD _xmlRef.name offsetof={} size={}",
                offset_of!(_xmlRef, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlRef>();
            let p = alloc_zeroed(layout) as *mut _xmlRef;
            println!(
                "FIELD _xmlRef.lineno offsetof={} size={}",
                offset_of!(_xmlRef, lineno),
                size_of_val(&(*p).lineno)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlSAXHandler sizeof={} alignof={}",
        size_of::<_xmlSAXHandler>(),
        align_of::<_xmlSAXHandler>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.internalSubset offsetof={} size={}",
                offset_of!(_xmlSAXHandler, internalSubset),
                size_of_val(&(*p).internalSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.isStandalone offsetof={} size={}",
                offset_of!(_xmlSAXHandler, isStandalone),
                size_of_val(&(*p).isStandalone)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.hasInternalSubset offsetof={} size={}",
                offset_of!(_xmlSAXHandler, hasInternalSubset),
                size_of_val(&(*p).hasInternalSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.hasExternalSubset offsetof={} size={}",
                offset_of!(_xmlSAXHandler, hasExternalSubset),
                size_of_val(&(*p).hasExternalSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.resolveEntity offsetof={} size={}",
                offset_of!(_xmlSAXHandler, resolveEntity),
                size_of_val(&(*p).resolveEntity)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.getEntity offsetof={} size={}",
                offset_of!(_xmlSAXHandler, getEntity),
                size_of_val(&(*p).getEntity)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.entityDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandler, entityDecl),
                size_of_val(&(*p).entityDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.notationDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandler, notationDecl),
                size_of_val(&(*p).notationDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.attributeDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandler, attributeDecl),
                size_of_val(&(*p).attributeDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.elementDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandler, elementDecl),
                size_of_val(&(*p).elementDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.unparsedEntityDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandler, unparsedEntityDecl),
                size_of_val(&(*p).unparsedEntityDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.setDocumentLocator offsetof={} size={}",
                offset_of!(_xmlSAXHandler, setDocumentLocator),
                size_of_val(&(*p).setDocumentLocator)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.startDocument offsetof={} size={}",
                offset_of!(_xmlSAXHandler, startDocument),
                size_of_val(&(*p).startDocument)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.endDocument offsetof={} size={}",
                offset_of!(_xmlSAXHandler, endDocument),
                size_of_val(&(*p).endDocument)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.startElement offsetof={} size={}",
                offset_of!(_xmlSAXHandler, startElement),
                size_of_val(&(*p).startElement)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.endElement offsetof={} size={}",
                offset_of!(_xmlSAXHandler, endElement),
                size_of_val(&(*p).endElement)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.reference offsetof={} size={}",
                offset_of!(_xmlSAXHandler, reference),
                size_of_val(&(*p).reference)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.characters offsetof={} size={}",
                offset_of!(_xmlSAXHandler, characters),
                size_of_val(&(*p).characters)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.ignorableWhitespace offsetof={} size={}",
                offset_of!(_xmlSAXHandler, ignorableWhitespace),
                size_of_val(&(*p).ignorableWhitespace)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.processingInstruction offsetof={} size={}",
                offset_of!(_xmlSAXHandler, processingInstruction),
                size_of_val(&(*p).processingInstruction)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.comment offsetof={} size={}",
                offset_of!(_xmlSAXHandler, comment),
                size_of_val(&(*p).comment)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.warning offsetof={} size={}",
                offset_of!(_xmlSAXHandler, warning),
                size_of_val(&(*p).warning)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.error offsetof={} size={}",
                offset_of!(_xmlSAXHandler, error),
                size_of_val(&(*p).error)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.fatalError offsetof={} size={}",
                offset_of!(_xmlSAXHandler, fatalError),
                size_of_val(&(*p).fatalError)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.getParameterEntity offsetof={} size={}",
                offset_of!(_xmlSAXHandler, getParameterEntity),
                size_of_val(&(*p).getParameterEntity)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.cdataBlock offsetof={} size={}",
                offset_of!(_xmlSAXHandler, cdataBlock),
                size_of_val(&(*p).cdataBlock)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.externalSubset offsetof={} size={}",
                offset_of!(_xmlSAXHandler, externalSubset),
                size_of_val(&(*p).externalSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.initialized offsetof={} size={}",
                offset_of!(_xmlSAXHandler, initialized),
                size_of_val(&(*p).initialized)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler._private offsetof={} size={}",
                offset_of!(_xmlSAXHandler, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.startElementNs offsetof={} size={}",
                offset_of!(_xmlSAXHandler, startElementNs),
                size_of_val(&(*p).startElementNs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.endElementNs offsetof={} size={}",
                offset_of!(_xmlSAXHandler, endElementNs),
                size_of_val(&(*p).endElementNs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandler>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandler;
            println!(
                "FIELD _xmlSAXHandler.serror offsetof={} size={}",
                offset_of!(_xmlSAXHandler, serror),
                size_of_val(&(*p).serror)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlSAXHandlerV1 sizeof={} alignof={}",
        size_of::<_xmlSAXHandlerV1>(),
        align_of::<_xmlSAXHandlerV1>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.internalSubset offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, internalSubset),
                size_of_val(&(*p).internalSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.isStandalone offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, isStandalone),
                size_of_val(&(*p).isStandalone)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.hasInternalSubset offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, hasInternalSubset),
                size_of_val(&(*p).hasInternalSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.hasExternalSubset offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, hasExternalSubset),
                size_of_val(&(*p).hasExternalSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.resolveEntity offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, resolveEntity),
                size_of_val(&(*p).resolveEntity)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.getEntity offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, getEntity),
                size_of_val(&(*p).getEntity)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.entityDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, entityDecl),
                size_of_val(&(*p).entityDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.notationDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, notationDecl),
                size_of_val(&(*p).notationDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.attributeDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, attributeDecl),
                size_of_val(&(*p).attributeDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.elementDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, elementDecl),
                size_of_val(&(*p).elementDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.unparsedEntityDecl offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, unparsedEntityDecl),
                size_of_val(&(*p).unparsedEntityDecl)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.setDocumentLocator offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, setDocumentLocator),
                size_of_val(&(*p).setDocumentLocator)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.startDocument offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, startDocument),
                size_of_val(&(*p).startDocument)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.endDocument offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, endDocument),
                size_of_val(&(*p).endDocument)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.startElement offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, startElement),
                size_of_val(&(*p).startElement)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.endElement offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, endElement),
                size_of_val(&(*p).endElement)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.reference offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, reference),
                size_of_val(&(*p).reference)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.characters offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, characters),
                size_of_val(&(*p).characters)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.ignorableWhitespace offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, ignorableWhitespace),
                size_of_val(&(*p).ignorableWhitespace)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.processingInstruction offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, processingInstruction),
                size_of_val(&(*p).processingInstruction)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.comment offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, comment),
                size_of_val(&(*p).comment)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.warning offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, warning),
                size_of_val(&(*p).warning)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.error offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, error),
                size_of_val(&(*p).error)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.fatalError offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, fatalError),
                size_of_val(&(*p).fatalError)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.getParameterEntity offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, getParameterEntity),
                size_of_val(&(*p).getParameterEntity)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.cdataBlock offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, cdataBlock),
                size_of_val(&(*p).cdataBlock)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.externalSubset offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, externalSubset),
                size_of_val(&(*p).externalSubset)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlSAXHandlerV1>();
            let p = alloc_zeroed(layout) as *mut _xmlSAXHandlerV1;
            println!(
                "FIELD _xmlSAXHandlerV1.initialized offsetof={} size={}",
                offset_of!(_xmlSAXHandlerV1, initialized),
                size_of_val(&(*p).initialized)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlValidCtxt sizeof={} alignof={}",
        size_of::<_xmlValidCtxt>(),
        align_of::<_xmlValidCtxt>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.userData offsetof={} size={}",
                offset_of!(_xmlValidCtxt, userData),
                size_of_val(&(*p).userData)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.error offsetof={} size={}",
                offset_of!(_xmlValidCtxt, error),
                size_of_val(&(*p).error)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.warning offsetof={} size={}",
                offset_of!(_xmlValidCtxt, warning),
                size_of_val(&(*p).warning)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.node offsetof={} size={}",
                offset_of!(_xmlValidCtxt, node),
                size_of_val(&(*p).node)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.nodeNr offsetof={} size={}",
                offset_of!(_xmlValidCtxt, nodeNr),
                size_of_val(&(*p).nodeNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.nodeMax offsetof={} size={}",
                offset_of!(_xmlValidCtxt, nodeMax),
                size_of_val(&(*p).nodeMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.nodeTab offsetof={} size={}",
                offset_of!(_xmlValidCtxt, nodeTab),
                size_of_val(&(*p).nodeTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.flags offsetof={} size={}",
                offset_of!(_xmlValidCtxt, flags),
                size_of_val(&(*p).flags)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.doc offsetof={} size={}",
                offset_of!(_xmlValidCtxt, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.valid offsetof={} size={}",
                offset_of!(_xmlValidCtxt, valid),
                size_of_val(&(*p).valid)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.vstate offsetof={} size={}",
                offset_of!(_xmlValidCtxt, vstate),
                size_of_val(&(*p).vstate)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.vstateNr offsetof={} size={}",
                offset_of!(_xmlValidCtxt, vstateNr),
                size_of_val(&(*p).vstateNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.vstateMax offsetof={} size={}",
                offset_of!(_xmlValidCtxt, vstateMax),
                size_of_val(&(*p).vstateMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.vstateTab offsetof={} size={}",
                offset_of!(_xmlValidCtxt, vstateTab),
                size_of_val(&(*p).vstateTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.am offsetof={} size={}",
                offset_of!(_xmlValidCtxt, am),
                size_of_val(&(*p).am)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlValidCtxt>();
            let p = alloc_zeroed(layout) as *mut _xmlValidCtxt;
            println!(
                "FIELD _xmlValidCtxt.state offsetof={} size={}",
                offset_of!(_xmlValidCtxt, state),
                size_of_val(&(*p).state)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlXPathContext sizeof={} alignof={}",
        size_of::<_xmlXPathContext>(),
        align_of::<_xmlXPathContext>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.doc offsetof={} size={}",
                offset_of!(_xmlXPathContext, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.node offsetof={} size={}",
                offset_of!(_xmlXPathContext, node),
                size_of_val(&(*p).node)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.nb_variables_unused offsetof={} size={}",
                offset_of!(_xmlXPathContext, nb_variables_unused),
                size_of_val(&(*p).nb_variables_unused)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.max_variables_unused offsetof={} size={}",
                offset_of!(_xmlXPathContext, max_variables_unused),
                size_of_val(&(*p).max_variables_unused)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.varHash offsetof={} size={}",
                offset_of!(_xmlXPathContext, varHash),
                size_of_val(&(*p).varHash)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.nb_types offsetof={} size={}",
                offset_of!(_xmlXPathContext, nb_types),
                size_of_val(&(*p).nb_types)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.max_types offsetof={} size={}",
                offset_of!(_xmlXPathContext, max_types),
                size_of_val(&(*p).max_types)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.types offsetof={} size={}",
                offset_of!(_xmlXPathContext, types),
                size_of_val(&(*p).types)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.nb_funcs_unused offsetof={} size={}",
                offset_of!(_xmlXPathContext, nb_funcs_unused),
                size_of_val(&(*p).nb_funcs_unused)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.max_funcs_unused offsetof={} size={}",
                offset_of!(_xmlXPathContext, max_funcs_unused),
                size_of_val(&(*p).max_funcs_unused)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.funcHash offsetof={} size={}",
                offset_of!(_xmlXPathContext, funcHash),
                size_of_val(&(*p).funcHash)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.nb_axis offsetof={} size={}",
                offset_of!(_xmlXPathContext, nb_axis),
                size_of_val(&(*p).nb_axis)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.max_axis offsetof={} size={}",
                offset_of!(_xmlXPathContext, max_axis),
                size_of_val(&(*p).max_axis)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.axis offsetof={} size={}",
                offset_of!(_xmlXPathContext, axis),
                size_of_val(&(*p).axis)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.namespaces offsetof={} size={}",
                offset_of!(_xmlXPathContext, namespaces),
                size_of_val(&(*p).namespaces)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.nsNr offsetof={} size={}",
                offset_of!(_xmlXPathContext, nsNr),
                size_of_val(&(*p).nsNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.user offsetof={} size={}",
                offset_of!(_xmlXPathContext, user),
                size_of_val(&(*p).user)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.contextSize offsetof={} size={}",
                offset_of!(_xmlXPathContext, contextSize),
                size_of_val(&(*p).contextSize)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.proximityPosition offsetof={} size={}",
                offset_of!(_xmlXPathContext, proximityPosition),
                size_of_val(&(*p).proximityPosition)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.xptr offsetof={} size={}",
                offset_of!(_xmlXPathContext, xptr),
                size_of_val(&(*p).xptr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.here offsetof={} size={}",
                offset_of!(_xmlXPathContext, here),
                size_of_val(&(*p).here)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.origin offsetof={} size={}",
                offset_of!(_xmlXPathContext, origin),
                size_of_val(&(*p).origin)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.nsHash offsetof={} size={}",
                offset_of!(_xmlXPathContext, nsHash),
                size_of_val(&(*p).nsHash)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.varLookupFunc offsetof={} size={}",
                offset_of!(_xmlXPathContext, varLookupFunc),
                size_of_val(&(*p).varLookupFunc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.varLookupData offsetof={} size={}",
                offset_of!(_xmlXPathContext, varLookupData),
                size_of_val(&(*p).varLookupData)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.extra offsetof={} size={}",
                offset_of!(_xmlXPathContext, extra),
                size_of_val(&(*p).extra)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.function offsetof={} size={}",
                offset_of!(_xmlXPathContext, function),
                size_of_val(&(*p).function)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.functionURI offsetof={} size={}",
                offset_of!(_xmlXPathContext, functionURI),
                size_of_val(&(*p).functionURI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.funcLookupFunc offsetof={} size={}",
                offset_of!(_xmlXPathContext, funcLookupFunc),
                size_of_val(&(*p).funcLookupFunc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.funcLookupData offsetof={} size={}",
                offset_of!(_xmlXPathContext, funcLookupData),
                size_of_val(&(*p).funcLookupData)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.tmpNsList offsetof={} size={}",
                offset_of!(_xmlXPathContext, tmpNsList),
                size_of_val(&(*p).tmpNsList)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.tmpNsNr offsetof={} size={}",
                offset_of!(_xmlXPathContext, tmpNsNr),
                size_of_val(&(*p).tmpNsNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.userData offsetof={} size={}",
                offset_of!(_xmlXPathContext, userData),
                size_of_val(&(*p).userData)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.error offsetof={} size={}",
                offset_of!(_xmlXPathContext, error),
                size_of_val(&(*p).error)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.lastError offsetof={} size={}",
                offset_of!(_xmlXPathContext, lastError),
                size_of_val(&(*p).lastError)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.debugNode offsetof={} size={}",
                offset_of!(_xmlXPathContext, debugNode),
                size_of_val(&(*p).debugNode)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.dict offsetof={} size={}",
                offset_of!(_xmlXPathContext, dict),
                size_of_val(&(*p).dict)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.flags offsetof={} size={}",
                offset_of!(_xmlXPathContext, flags),
                size_of_val(&(*p).flags)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.cache offsetof={} size={}",
                offset_of!(_xmlXPathContext, cache),
                size_of_val(&(*p).cache)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.opLimit offsetof={} size={}",
                offset_of!(_xmlXPathContext, opLimit),
                size_of_val(&(*p).opLimit)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.opCount offsetof={} size={}",
                offset_of!(_xmlXPathContext, opCount),
                size_of_val(&(*p).opCount)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathContext>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathContext;
            println!(
                "FIELD _xmlXPathContext.depth offsetof={} size={}",
                offset_of!(_xmlXPathContext, depth),
                size_of_val(&(*p).depth)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xmlXPathObject sizeof={} alignof={}",
        size_of::<_xmlXPathObject>(),
        align_of::<_xmlXPathObject>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathObject>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathObject;
            println!(
                "FIELD _xmlXPathObject.type_ offsetof={} size={}",
                offset_of!(_xmlXPathObject, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathObject>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathObject;
            println!(
                "FIELD _xmlXPathObject.nodesetval offsetof={} size={}",
                offset_of!(_xmlXPathObject, nodesetval),
                size_of_val(&(*p).nodesetval)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathObject>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathObject;
            println!(
                "FIELD _xmlXPathObject.boolval offsetof={} size={}",
                offset_of!(_xmlXPathObject, boolval),
                size_of_val(&(*p).boolval)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathObject>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathObject;
            println!(
                "FIELD _xmlXPathObject.floatval offsetof={} size={}",
                offset_of!(_xmlXPathObject, floatval),
                size_of_val(&(*p).floatval)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathObject>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathObject;
            println!(
                "FIELD _xmlXPathObject.stringval offsetof={} size={}",
                offset_of!(_xmlXPathObject, stringval),
                size_of_val(&(*p).stringval)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathObject>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathObject;
            println!(
                "FIELD _xmlXPathObject.user offsetof={} size={}",
                offset_of!(_xmlXPathObject, user),
                size_of_val(&(*p).user)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathObject>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathObject;
            println!(
                "FIELD _xmlXPathObject.index offsetof={} size={}",
                offset_of!(_xmlXPathObject, index),
                size_of_val(&(*p).index)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathObject>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathObject;
            println!(
                "FIELD _xmlXPathObject.user2 offsetof={} size={}",
                offset_of!(_xmlXPathObject, user2),
                size_of_val(&(*p).user2)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xmlXPathObject>();
            let p = alloc_zeroed(layout) as *mut _xmlXPathObject;
            println!(
                "FIELD _xmlXPathObject.index2 offsetof={} size={}",
                offset_of!(_xmlXPathObject, index2),
                size_of_val(&(*p).index2)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltAttrSet sizeof={} alignof={}",
        size_of::<_xsltAttrSet>(),
        align_of::<_xsltAttrSet>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltAttrSet>();
            let p = alloc_zeroed(layout) as *mut _xsltAttrSet;
            println!(
                "FIELD _xsltAttrSet.next offsetof={} size={}",
                offset_of!(_xsltAttrSet, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltAttrSet>();
            let p = alloc_zeroed(layout) as *mut _xsltAttrSet;
            println!(
                "FIELD _xsltAttrSet.name offsetof={} size={}",
                offset_of!(_xsltAttrSet, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltAttrSet>();
            let p = alloc_zeroed(layout) as *mut _xsltAttrSet;
            println!(
                "FIELD _xsltAttrSet.ns offsetof={} size={}",
                offset_of!(_xsltAttrSet, ns),
                size_of_val(&(*p).ns)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltAttrSet>();
            let p = alloc_zeroed(layout) as *mut _xsltAttrSet;
            println!(
                "FIELD _xsltAttrSet.inst offsetof={} size={}",
                offset_of!(_xsltAttrSet, inst),
                size_of_val(&(*p).inst)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltAttrSet>();
            let p = alloc_zeroed(layout) as *mut _xsltAttrSet;
            println!(
                "FIELD _xsltAttrSet.style offsetof={} size={}",
                offset_of!(_xsltAttrSet, style),
                size_of_val(&(*p).style)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltAttrSet>();
            let p = alloc_zeroed(layout) as *mut _xsltAttrSet;
            println!(
                "FIELD _xsltAttrSet.depth offsetof={} size={}",
                offset_of!(_xsltAttrSet, depth),
                size_of_val(&(*p).depth)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltDecimalFormat sizeof={} alignof={}",
        size_of::<_xsltDecimalFormat>(),
        align_of::<_xsltDecimalFormat>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.next offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.name offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.digit offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, digit),
                size_of_val(&(*p).digit)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.patternSeparator offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, patternSeparator),
                size_of_val(&(*p).patternSeparator)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.minusSign offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, minusSign),
                size_of_val(&(*p).minusSign)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.infinity offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, infinity),
                size_of_val(&(*p).infinity)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.noNumber offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, noNumber),
                size_of_val(&(*p).noNumber)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.decimalPoint offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, decimalPoint),
                size_of_val(&(*p).decimalPoint)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.grouping offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, grouping),
                size_of_val(&(*p).grouping)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.percent offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, percent),
                size_of_val(&(*p).percent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.permille offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, permille),
                size_of_val(&(*p).permille)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.zeroDigit offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, zeroDigit),
                size_of_val(&(*p).zeroDigit)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDecimalFormat>();
            let p = alloc_zeroed(layout) as *mut _xsltDecimalFormat;
            println!(
                "FIELD _xsltDecimalFormat.nsUri offsetof={} size={}",
                offset_of!(_xsltDecimalFormat, nsUri),
                size_of_val(&(*p).nsUri)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltDocument sizeof={} alignof={}",
        size_of::<_xsltDocument>(),
        align_of::<_xsltDocument>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltDocument>();
            let p = alloc_zeroed(layout) as *mut _xsltDocument;
            println!(
                "FIELD _xsltDocument.next offsetof={} size={}",
                offset_of!(_xsltDocument, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDocument>();
            let p = alloc_zeroed(layout) as *mut _xsltDocument;
            println!(
                "FIELD _xsltDocument.main offsetof={} size={}",
                offset_of!(_xsltDocument, main),
                size_of_val(&(*p).main)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDocument>();
            let p = alloc_zeroed(layout) as *mut _xsltDocument;
            println!(
                "FIELD _xsltDocument.doc offsetof={} size={}",
                offset_of!(_xsltDocument, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDocument>();
            let p = alloc_zeroed(layout) as *mut _xsltDocument;
            println!(
                "FIELD _xsltDocument.keys offsetof={} size={}",
                offset_of!(_xsltDocument, keys),
                size_of_val(&(*p).keys)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDocument>();
            let p = alloc_zeroed(layout) as *mut _xsltDocument;
            println!(
                "FIELD _xsltDocument.includes offsetof={} size={}",
                offset_of!(_xsltDocument, includes),
                size_of_val(&(*p).includes)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDocument>();
            let p = alloc_zeroed(layout) as *mut _xsltDocument;
            println!(
                "FIELD _xsltDocument.preproc offsetof={} size={}",
                offset_of!(_xsltDocument, preproc),
                size_of_val(&(*p).preproc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltDocument>();
            let p = alloc_zeroed(layout) as *mut _xsltDocument;
            println!(
                "FIELD _xsltDocument.nbKeysComputed offsetof={} size={}",
                offset_of!(_xsltDocument, nbKeysComputed),
                size_of_val(&(*p).nbKeysComputed)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltKeyDef sizeof={} alignof={}",
        size_of::<_xsltKeyDef>(),
        align_of::<_xsltKeyDef>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.next offsetof={} size={}",
                offset_of!(_xsltKeyDef, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.inst offsetof={} size={}",
                offset_of!(_xsltKeyDef, inst),
                size_of_val(&(*p).inst)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.name offsetof={} size={}",
                offset_of!(_xsltKeyDef, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.nameURI offsetof={} size={}",
                offset_of!(_xsltKeyDef, nameURI),
                size_of_val(&(*p).nameURI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.r#match offsetof={} size={}",
                offset_of!(_xsltKeyDef, r#match),
                size_of_val(&(*p).r#match)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.r#use offsetof={} size={}",
                offset_of!(_xsltKeyDef, r#use),
                size_of_val(&(*p).r#use)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.comp offsetof={} size={}",
                offset_of!(_xsltKeyDef, comp),
                size_of_val(&(*p).comp)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.usecomp offsetof={} size={}",
                offset_of!(_xsltKeyDef, usecomp),
                size_of_val(&(*p).usecomp)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.nsList offsetof={} size={}",
                offset_of!(_xsltKeyDef, nsList),
                size_of_val(&(*p).nsList)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyDef>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyDef;
            println!(
                "FIELD _xsltKeyDef.nsNr offsetof={} size={}",
                offset_of!(_xsltKeyDef, nsNr),
                size_of_val(&(*p).nsNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltKeyTable sizeof={} alignof={}",
        size_of::<_xsltKeyTable>(),
        align_of::<_xsltKeyTable>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyTable>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyTable;
            println!(
                "FIELD _xsltKeyTable.next offsetof={} size={}",
                offset_of!(_xsltKeyTable, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyTable>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyTable;
            println!(
                "FIELD _xsltKeyTable.name offsetof={} size={}",
                offset_of!(_xsltKeyTable, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyTable>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyTable;
            println!(
                "FIELD _xsltKeyTable.nameURI offsetof={} size={}",
                offset_of!(_xsltKeyTable, nameURI),
                size_of_val(&(*p).nameURI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltKeyTable>();
            let p = alloc_zeroed(layout) as *mut _xsltKeyTable;
            println!(
                "FIELD _xsltKeyTable.keys offsetof={} size={}",
                offset_of!(_xsltKeyTable, keys),
                size_of_val(&(*p).keys)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltNsAlias sizeof={} alignof={}",
        size_of::<_xsltNsAlias>(),
        align_of::<_xsltNsAlias>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltNsAlias>();
            let p = alloc_zeroed(layout) as *mut _xsltNsAlias;
            println!(
                "FIELD _xsltNsAlias.next offsetof={} size={}",
                offset_of!(_xsltNsAlias, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltNsAlias>();
            let p = alloc_zeroed(layout) as *mut _xsltNsAlias;
            println!(
                "FIELD _xsltNsAlias.resultNs offsetof={} size={}",
                offset_of!(_xsltNsAlias, resultNs),
                size_of_val(&(*p).resultNs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltNsAlias>();
            let p = alloc_zeroed(layout) as *mut _xsltNsAlias;
            println!(
                "FIELD _xsltNsAlias.styleNs offsetof={} size={}",
                offset_of!(_xsltNsAlias, styleNs),
                size_of_val(&(*p).styleNs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltSort sizeof={} alignof={}",
        size_of::<_xsltSort>(),
        align_of::<_xsltSort>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.next offsetof={} size={}",
                offset_of!(_xsltSort, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.inst offsetof={} size={}",
                offset_of!(_xsltSort, inst),
                size_of_val(&(*p).inst)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.select offsetof={} size={}",
                offset_of!(_xsltSort, select),
                size_of_val(&(*p).select)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.lang offsetof={} size={}",
                offset_of!(_xsltSort, lang),
                size_of_val(&(*p).lang)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.dataType offsetof={} size={}",
                offset_of!(_xsltSort, dataType),
                size_of_val(&(*p).dataType)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.order offsetof={} size={}",
                offset_of!(_xsltSort, order),
                size_of_val(&(*p).order)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.caseOrder offsetof={} size={}",
                offset_of!(_xsltSort, caseOrder),
                size_of_val(&(*p).caseOrder)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.isText offsetof={} size={}",
                offset_of!(_xsltSort, isText),
                size_of_val(&(*p).isText)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.hasConst offsetof={} size={}",
                offset_of!(_xsltSort, hasConst),
                size_of_val(&(*p).hasConst)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.locale offsetof={} size={}",
                offset_of!(_xsltSort, locale),
                size_of_val(&(*p).locale)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.style offsetof={} size={}",
                offset_of!(_xsltSort, style),
                size_of_val(&(*p).style)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltSort>();
            let p = alloc_zeroed(layout) as *mut _xsltSort;
            println!(
                "FIELD _xsltSort.depth offsetof={} size={}",
                offset_of!(_xsltSort, depth),
                size_of_val(&(*p).depth)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltStackElem sizeof={} alignof={}",
        size_of::<_xsltStackElem>(),
        align_of::<_xsltStackElem>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.next offsetof={} size={}",
                offset_of!(_xsltStackElem, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.comp offsetof={} size={}",
                offset_of!(_xsltStackElem, comp),
                size_of_val(&(*p).comp)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.computed offsetof={} size={}",
                offset_of!(_xsltStackElem, computed),
                size_of_val(&(*p).computed)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.name offsetof={} size={}",
                offset_of!(_xsltStackElem, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.nameURI offsetof={} size={}",
                offset_of!(_xsltStackElem, nameURI),
                size_of_val(&(*p).nameURI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.select offsetof={} size={}",
                offset_of!(_xsltStackElem, select),
                size_of_val(&(*p).select)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.tree offsetof={} size={}",
                offset_of!(_xsltStackElem, tree),
                size_of_val(&(*p).tree)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.value offsetof={} size={}",
                offset_of!(_xsltStackElem, value),
                size_of_val(&(*p).value)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.fragment offsetof={} size={}",
                offset_of!(_xsltStackElem, fragment),
                size_of_val(&(*p).fragment)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.level offsetof={} size={}",
                offset_of!(_xsltStackElem, level),
                size_of_val(&(*p).level)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.context offsetof={} size={}",
                offset_of!(_xsltStackElem, context),
                size_of_val(&(*p).context)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStackElem>();
            let p = alloc_zeroed(layout) as *mut _xsltStackElem;
            println!(
                "FIELD _xsltStackElem.flags offsetof={} size={}",
                offset_of!(_xsltStackElem, flags),
                size_of_val(&(*p).flags)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltStylesheet sizeof={} alignof={}",
        size_of::<_xsltStylesheet>(),
        align_of::<_xsltStylesheet>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.parent offsetof={} size={}",
                offset_of!(_xsltStylesheet, parent),
                size_of_val(&(*p).parent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.next offsetof={} size={}",
                offset_of!(_xsltStylesheet, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.imports offsetof={} size={}",
                offset_of!(_xsltStylesheet, imports),
                size_of_val(&(*p).imports)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.docList offsetof={} size={}",
                offset_of!(_xsltStylesheet, docList),
                size_of_val(&(*p).docList)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.doc offsetof={} size={}",
                offset_of!(_xsltStylesheet, doc),
                size_of_val(&(*p).doc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.stripSpaces offsetof={} size={}",
                offset_of!(_xsltStylesheet, stripSpaces),
                size_of_val(&(*p).stripSpaces)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.stripAll offsetof={} size={}",
                offset_of!(_xsltStylesheet, stripAll),
                size_of_val(&(*p).stripAll)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.cdataSection offsetof={} size={}",
                offset_of!(_xsltStylesheet, cdataSection),
                size_of_val(&(*p).cdataSection)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.variables offsetof={} size={}",
                offset_of!(_xsltStylesheet, variables),
                size_of_val(&(*p).variables)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.templates offsetof={} size={}",
                offset_of!(_xsltStylesheet, templates),
                size_of_val(&(*p).templates)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.templatesHash offsetof={} size={}",
                offset_of!(_xsltStylesheet, templatesHash),
                size_of_val(&(*p).templatesHash)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.rootMatch offsetof={} size={}",
                offset_of!(_xsltStylesheet, rootMatch),
                size_of_val(&(*p).rootMatch)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.keyMatch offsetof={} size={}",
                offset_of!(_xsltStylesheet, keyMatch),
                size_of_val(&(*p).keyMatch)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.elemMatch offsetof={} size={}",
                offset_of!(_xsltStylesheet, elemMatch),
                size_of_val(&(*p).elemMatch)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.attrMatch offsetof={} size={}",
                offset_of!(_xsltStylesheet, attrMatch),
                size_of_val(&(*p).attrMatch)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.parentMatch offsetof={} size={}",
                offset_of!(_xsltStylesheet, parentMatch),
                size_of_val(&(*p).parentMatch)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.textMatch offsetof={} size={}",
                offset_of!(_xsltStylesheet, textMatch),
                size_of_val(&(*p).textMatch)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.piMatch offsetof={} size={}",
                offset_of!(_xsltStylesheet, piMatch),
                size_of_val(&(*p).piMatch)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.commentMatch offsetof={} size={}",
                offset_of!(_xsltStylesheet, commentMatch),
                size_of_val(&(*p).commentMatch)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.nsAliases offsetof={} size={}",
                offset_of!(_xsltStylesheet, nsAliases),
                size_of_val(&(*p).nsAliases)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.attributeSets offsetof={} size={}",
                offset_of!(_xsltStylesheet, attributeSets),
                size_of_val(&(*p).attributeSets)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.nsHash offsetof={} size={}",
                offset_of!(_xsltStylesheet, nsHash),
                size_of_val(&(*p).nsHash)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.nsDefs offsetof={} size={}",
                offset_of!(_xsltStylesheet, nsDefs),
                size_of_val(&(*p).nsDefs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.keys offsetof={} size={}",
                offset_of!(_xsltStylesheet, keys),
                size_of_val(&(*p).keys)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.method offsetof={} size={}",
                offset_of!(_xsltStylesheet, method),
                size_of_val(&(*p).method)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.methodURI offsetof={} size={}",
                offset_of!(_xsltStylesheet, methodURI),
                size_of_val(&(*p).methodURI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.version offsetof={} size={}",
                offset_of!(_xsltStylesheet, version),
                size_of_val(&(*p).version)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.encoding offsetof={} size={}",
                offset_of!(_xsltStylesheet, encoding),
                size_of_val(&(*p).encoding)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.omitXmlDeclaration offsetof={} size={}",
                offset_of!(_xsltStylesheet, omitXmlDeclaration),
                size_of_val(&(*p).omitXmlDeclaration)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.decimalFormat offsetof={} size={}",
                offset_of!(_xsltStylesheet, decimalFormat),
                size_of_val(&(*p).decimalFormat)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.standalone offsetof={} size={}",
                offset_of!(_xsltStylesheet, standalone),
                size_of_val(&(*p).standalone)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.doctypePublic offsetof={} size={}",
                offset_of!(_xsltStylesheet, doctypePublic),
                size_of_val(&(*p).doctypePublic)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.doctypeSystem offsetof={} size={}",
                offset_of!(_xsltStylesheet, doctypeSystem),
                size_of_val(&(*p).doctypeSystem)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.indent offsetof={} size={}",
                offset_of!(_xsltStylesheet, indent),
                size_of_val(&(*p).indent)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.mediaType offsetof={} size={}",
                offset_of!(_xsltStylesheet, mediaType),
                size_of_val(&(*p).mediaType)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.preComps offsetof={} size={}",
                offset_of!(_xsltStylesheet, preComps),
                size_of_val(&(*p).preComps)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.warnings offsetof={} size={}",
                offset_of!(_xsltStylesheet, warnings),
                size_of_val(&(*p).warnings)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.errors offsetof={} size={}",
                offset_of!(_xsltStylesheet, errors),
                size_of_val(&(*p).errors)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.exclPrefix offsetof={} size={}",
                offset_of!(_xsltStylesheet, exclPrefix),
                size_of_val(&(*p).exclPrefix)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.exclPrefixTab offsetof={} size={}",
                offset_of!(_xsltStylesheet, exclPrefixTab),
                size_of_val(&(*p).exclPrefixTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.exclPrefixNr offsetof={} size={}",
                offset_of!(_xsltStylesheet, exclPrefixNr),
                size_of_val(&(*p).exclPrefixNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.exclPrefixMax offsetof={} size={}",
                offset_of!(_xsltStylesheet, exclPrefixMax),
                size_of_val(&(*p).exclPrefixMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet._private offsetof={} size={}",
                offset_of!(_xsltStylesheet, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.extInfos offsetof={} size={}",
                offset_of!(_xsltStylesheet, extInfos),
                size_of_val(&(*p).extInfos)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.extrasNr offsetof={} size={}",
                offset_of!(_xsltStylesheet, extrasNr),
                size_of_val(&(*p).extrasNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.includes offsetof={} size={}",
                offset_of!(_xsltStylesheet, includes),
                size_of_val(&(*p).includes)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.dict offsetof={} size={}",
                offset_of!(_xsltStylesheet, dict),
                size_of_val(&(*p).dict)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.attVTs offsetof={} size={}",
                offset_of!(_xsltStylesheet, attVTs),
                size_of_val(&(*p).attVTs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.defaultAlias offsetof={} size={}",
                offset_of!(_xsltStylesheet, defaultAlias),
                size_of_val(&(*p).defaultAlias)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.nopreproc offsetof={} size={}",
                offset_of!(_xsltStylesheet, nopreproc),
                size_of_val(&(*p).nopreproc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.internalized offsetof={} size={}",
                offset_of!(_xsltStylesheet, internalized),
                size_of_val(&(*p).internalized)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.literal_result offsetof={} size={}",
                offset_of!(_xsltStylesheet, literal_result),
                size_of_val(&(*p).literal_result)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.principal offsetof={} size={}",
                offset_of!(_xsltStylesheet, principal),
                size_of_val(&(*p).principal)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.forwards_compatible offsetof={} size={}",
                offset_of!(_xsltStylesheet, forwards_compatible),
                size_of_val(&(*p).forwards_compatible)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.namedTemplates offsetof={} size={}",
                offset_of!(_xsltStylesheet, namedTemplates),
                size_of_val(&(*p).namedTemplates)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.xpathCtxt offsetof={} size={}",
                offset_of!(_xsltStylesheet, xpathCtxt),
                size_of_val(&(*p).xpathCtxt)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.opLimit offsetof={} size={}",
                offset_of!(_xsltStylesheet, opLimit),
                size_of_val(&(*p).opLimit)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltStylesheet>();
            let p = alloc_zeroed(layout) as *mut _xsltStylesheet;
            println!(
                "FIELD _xsltStylesheet.opCount offsetof={} size={}",
                offset_of!(_xsltStylesheet, opCount),
                size_of_val(&(*p).opCount)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltTemplate sizeof={} alignof={}",
        size_of::<_xsltTemplate>(),
        align_of::<_xsltTemplate>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.next offsetof={} size={}",
                offset_of!(_xsltTemplate, next),
                size_of_val(&(*p).next)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.style offsetof={} size={}",
                offset_of!(_xsltTemplate, style),
                size_of_val(&(*p).style)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.r#match offsetof={} size={}",
                offset_of!(_xsltTemplate, r#match),
                size_of_val(&(*p).r#match)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.priority offsetof={} size={}",
                offset_of!(_xsltTemplate, priority),
                size_of_val(&(*p).priority)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.name offsetof={} size={}",
                offset_of!(_xsltTemplate, name),
                size_of_val(&(*p).name)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.nameURI offsetof={} size={}",
                offset_of!(_xsltTemplate, nameURI),
                size_of_val(&(*p).nameURI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.mode offsetof={} size={}",
                offset_of!(_xsltTemplate, mode),
                size_of_val(&(*p).mode)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.modeURI offsetof={} size={}",
                offset_of!(_xsltTemplate, modeURI),
                size_of_val(&(*p).modeURI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.content offsetof={} size={}",
                offset_of!(_xsltTemplate, content),
                size_of_val(&(*p).content)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.elem offsetof={} size={}",
                offset_of!(_xsltTemplate, elem),
                size_of_val(&(*p).elem)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.inheritedNsNr offsetof={} size={}",
                offset_of!(_xsltTemplate, inheritedNsNr),
                size_of_val(&(*p).inheritedNsNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.inheritedNs offsetof={} size={}",
                offset_of!(_xsltTemplate, inheritedNs),
                size_of_val(&(*p).inheritedNs)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.nbCalls offsetof={} size={}",
                offset_of!(_xsltTemplate, nbCalls),
                size_of_val(&(*p).nbCalls)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.time offsetof={} size={}",
                offset_of!(_xsltTemplate, time),
                size_of_val(&(*p).time)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.params offsetof={} size={}",
                offset_of!(_xsltTemplate, params),
                size_of_val(&(*p).params)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.templNr offsetof={} size={}",
                offset_of!(_xsltTemplate, templNr),
                size_of_val(&(*p).templNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.templMax offsetof={} size={}",
                offset_of!(_xsltTemplate, templMax),
                size_of_val(&(*p).templMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.templCalledTab offsetof={} size={}",
                offset_of!(_xsltTemplate, templCalledTab),
                size_of_val(&(*p).templCalledTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.templCountTab offsetof={} size={}",
                offset_of!(_xsltTemplate, templCountTab),
                size_of_val(&(*p).templCountTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTemplate>();
            let p = alloc_zeroed(layout) as *mut _xsltTemplate;
            println!(
                "FIELD _xsltTemplate.position offsetof={} size={}",
                offset_of!(_xsltTemplate, position),
                size_of_val(&(*p).position)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT _xsltTransformContext sizeof={} alignof={}",
        size_of::<_xsltTransformContext>(),
        align_of::<_xsltTransformContext>()
    );
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.style offsetof={} size={}",
                offset_of!(_xsltTransformContext, style),
                size_of_val(&(*p).style)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.type_ offsetof={} size={}",
                offset_of!(_xsltTransformContext, type_),
                size_of_val(&(*p).type_)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.templ offsetof={} size={}",
                offset_of!(_xsltTransformContext, templ),
                size_of_val(&(*p).templ)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.templNr offsetof={} size={}",
                offset_of!(_xsltTransformContext, templNr),
                size_of_val(&(*p).templNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.templMax offsetof={} size={}",
                offset_of!(_xsltTransformContext, templMax),
                size_of_val(&(*p).templMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.templTab offsetof={} size={}",
                offset_of!(_xsltTransformContext, templTab),
                size_of_val(&(*p).templTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.vars offsetof={} size={}",
                offset_of!(_xsltTransformContext, vars),
                size_of_val(&(*p).vars)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.varsNr offsetof={} size={}",
                offset_of!(_xsltTransformContext, varsNr),
                size_of_val(&(*p).varsNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.varsMax offsetof={} size={}",
                offset_of!(_xsltTransformContext, varsMax),
                size_of_val(&(*p).varsMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.varsTab offsetof={} size={}",
                offset_of!(_xsltTransformContext, varsTab),
                size_of_val(&(*p).varsTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.varsBase offsetof={} size={}",
                offset_of!(_xsltTransformContext, varsBase),
                size_of_val(&(*p).varsBase)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.extFunctions offsetof={} size={}",
                offset_of!(_xsltTransformContext, extFunctions),
                size_of_val(&(*p).extFunctions)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.extElements offsetof={} size={}",
                offset_of!(_xsltTransformContext, extElements),
                size_of_val(&(*p).extElements)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.extInfos offsetof={} size={}",
                offset_of!(_xsltTransformContext, extInfos),
                size_of_val(&(*p).extInfos)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.mode offsetof={} size={}",
                offset_of!(_xsltTransformContext, mode),
                size_of_val(&(*p).mode)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.modeURI offsetof={} size={}",
                offset_of!(_xsltTransformContext, modeURI),
                size_of_val(&(*p).modeURI)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.docList offsetof={} size={}",
                offset_of!(_xsltTransformContext, docList),
                size_of_val(&(*p).docList)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.document offsetof={} size={}",
                offset_of!(_xsltTransformContext, document),
                size_of_val(&(*p).document)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.node offsetof={} size={}",
                offset_of!(_xsltTransformContext, node),
                size_of_val(&(*p).node)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.nodeList offsetof={} size={}",
                offset_of!(_xsltTransformContext, nodeList),
                size_of_val(&(*p).nodeList)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.output offsetof={} size={}",
                offset_of!(_xsltTransformContext, output),
                size_of_val(&(*p).output)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.insert offsetof={} size={}",
                offset_of!(_xsltTransformContext, insert),
                size_of_val(&(*p).insert)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.xpathCtxt offsetof={} size={}",
                offset_of!(_xsltTransformContext, xpathCtxt),
                size_of_val(&(*p).xpathCtxt)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.state offsetof={} size={}",
                offset_of!(_xsltTransformContext, state),
                size_of_val(&(*p).state)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.globalVars offsetof={} size={}",
                offset_of!(_xsltTransformContext, globalVars),
                size_of_val(&(*p).globalVars)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.inst offsetof={} size={}",
                offset_of!(_xsltTransformContext, inst),
                size_of_val(&(*p).inst)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.xinclude offsetof={} size={}",
                offset_of!(_xsltTransformContext, xinclude),
                size_of_val(&(*p).xinclude)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.outputFile offsetof={} size={}",
                offset_of!(_xsltTransformContext, outputFile),
                size_of_val(&(*p).outputFile)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.profile offsetof={} size={}",
                offset_of!(_xsltTransformContext, profile),
                size_of_val(&(*p).profile)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.prof offsetof={} size={}",
                offset_of!(_xsltTransformContext, prof),
                size_of_val(&(*p).prof)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.profNr offsetof={} size={}",
                offset_of!(_xsltTransformContext, profNr),
                size_of_val(&(*p).profNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.profMax offsetof={} size={}",
                offset_of!(_xsltTransformContext, profMax),
                size_of_val(&(*p).profMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.profTab offsetof={} size={}",
                offset_of!(_xsltTransformContext, profTab),
                size_of_val(&(*p).profTab)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext._private offsetof={} size={}",
                offset_of!(_xsltTransformContext, _private),
                size_of_val(&(*p)._private)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.extrasNr offsetof={} size={}",
                offset_of!(_xsltTransformContext, extrasNr),
                size_of_val(&(*p).extrasNr)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.extrasMax offsetof={} size={}",
                offset_of!(_xsltTransformContext, extrasMax),
                size_of_val(&(*p).extrasMax)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.extras offsetof={} size={}",
                offset_of!(_xsltTransformContext, extras),
                size_of_val(&(*p).extras)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.styleList offsetof={} size={}",
                offset_of!(_xsltTransformContext, styleList),
                size_of_val(&(*p).styleList)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.sec offsetof={} size={}",
                offset_of!(_xsltTransformContext, sec),
                size_of_val(&(*p).sec)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.error offsetof={} size={}",
                offset_of!(_xsltTransformContext, error),
                size_of_val(&(*p).error)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.errctx offsetof={} size={}",
                offset_of!(_xsltTransformContext, errctx),
                size_of_val(&(*p).errctx)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.sortfunc offsetof={} size={}",
                offset_of!(_xsltTransformContext, sortfunc),
                size_of_val(&(*p).sortfunc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.tmpRVT offsetof={} size={}",
                offset_of!(_xsltTransformContext, tmpRVT),
                size_of_val(&(*p).tmpRVT)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.persistRVT offsetof={} size={}",
                offset_of!(_xsltTransformContext, persistRVT),
                size_of_val(&(*p).persistRVT)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.ctxtflags offsetof={} size={}",
                offset_of!(_xsltTransformContext, ctxtflags),
                size_of_val(&(*p).ctxtflags)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.lasttext offsetof={} size={}",
                offset_of!(_xsltTransformContext, lasttext),
                size_of_val(&(*p).lasttext)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.lasttsize offsetof={} size={}",
                offset_of!(_xsltTransformContext, lasttsize),
                size_of_val(&(*p).lasttsize)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.lasttuse offsetof={} size={}",
                offset_of!(_xsltTransformContext, lasttuse),
                size_of_val(&(*p).lasttuse)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.debugStatus offsetof={} size={}",
                offset_of!(_xsltTransformContext, debugStatus),
                size_of_val(&(*p).debugStatus)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.traceCode offsetof={} size={}",
                offset_of!(_xsltTransformContext, traceCode),
                size_of_val(&(*p).traceCode)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.parserOptions offsetof={} size={}",
                offset_of!(_xsltTransformContext, parserOptions),
                size_of_val(&(*p).parserOptions)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.dict offsetof={} size={}",
                offset_of!(_xsltTransformContext, dict),
                size_of_val(&(*p).dict)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.tmpDoc offsetof={} size={}",
                offset_of!(_xsltTransformContext, tmpDoc),
                size_of_val(&(*p).tmpDoc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.internalized offsetof={} size={}",
                offset_of!(_xsltTransformContext, internalized),
                size_of_val(&(*p).internalized)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.nbKeys offsetof={} size={}",
                offset_of!(_xsltTransformContext, nbKeys),
                size_of_val(&(*p).nbKeys)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.hasTemplKeyPatterns offsetof={} size={}",
                offset_of!(_xsltTransformContext, hasTemplKeyPatterns),
                size_of_val(&(*p).hasTemplKeyPatterns)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.currentTemplateRule offsetof={} size={}",
                offset_of!(_xsltTransformContext, currentTemplateRule),
                size_of_val(&(*p).currentTemplateRule)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.initialContextNode offsetof={} size={}",
                offset_of!(_xsltTransformContext, initialContextNode),
                size_of_val(&(*p).initialContextNode)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.initialContextDoc offsetof={} size={}",
                offset_of!(_xsltTransformContext, initialContextDoc),
                size_of_val(&(*p).initialContextDoc)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.cache offsetof={} size={}",
                offset_of!(_xsltTransformContext, cache),
                size_of_val(&(*p).cache)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.contextVariable offsetof={} size={}",
                offset_of!(_xsltTransformContext, contextVariable),
                size_of_val(&(*p).contextVariable)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.localRVT offsetof={} size={}",
                offset_of!(_xsltTransformContext, localRVT),
                size_of_val(&(*p).localRVT)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.localRVTBase offsetof={} size={}",
                offset_of!(_xsltTransformContext, localRVTBase),
                size_of_val(&(*p).localRVTBase)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.keyInitLevel offsetof={} size={}",
                offset_of!(_xsltTransformContext, keyInitLevel),
                size_of_val(&(*p).keyInitLevel)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.depth offsetof={} size={}",
                offset_of!(_xsltTransformContext, depth),
                size_of_val(&(*p).depth)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.maxTemplateDepth offsetof={} size={}",
                offset_of!(_xsltTransformContext, maxTemplateDepth),
                size_of_val(&(*p).maxTemplateDepth)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.maxTemplateVars offsetof={} size={}",
                offset_of!(_xsltTransformContext, maxTemplateVars),
                size_of_val(&(*p).maxTemplateVars)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.opLimit offsetof={} size={}",
                offset_of!(_xsltTransformContext, opLimit),
                size_of_val(&(*p).opLimit)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.opCount offsetof={} size={}",
                offset_of!(_xsltTransformContext, opCount),
                size_of_val(&(*p).opCount)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.sourceDocDirty offsetof={} size={}",
                offset_of!(_xsltTransformContext, sourceDocDirty),
                size_of_val(&(*p).sourceDocDirty)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.currentId offsetof={} size={}",
                offset_of!(_xsltTransformContext, currentId),
                size_of_val(&(*p).currentId)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.newLocale offsetof={} size={}",
                offset_of!(_xsltTransformContext, newLocale),
                size_of_val(&(*p).newLocale)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.freeLocale offsetof={} size={}",
                offset_of!(_xsltTransformContext, freeLocale),
                size_of_val(&(*p).freeLocale)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<_xsltTransformContext>();
            let p = alloc_zeroed(layout) as *mut _xsltTransformContext;
            println!(
                "FIELD _xsltTransformContext.genSortKey offsetof={} size={}",
                offset_of!(_xsltTransformContext, genSortKey),
                size_of_val(&(*p).genSortKey)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT xmlChLRange sizeof={} alignof={}",
        size_of::<xmlChLRange>(),
        align_of::<xmlChLRange>()
    );
    {
        unsafe {
            let layout = Layout::new::<xmlChLRange>();
            let p = alloc_zeroed(layout) as *mut xmlChLRange;
            println!(
                "FIELD xmlChLRange.low offsetof={} size={}",
                offset_of!(xmlChLRange, low),
                size_of_val(&(*p).low)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<xmlChLRange>();
            let p = alloc_zeroed(layout) as *mut xmlChLRange;
            println!(
                "FIELD xmlChLRange.high offsetof={} size={}",
                offset_of!(xmlChLRange, high),
                size_of_val(&(*p).high)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT xmlChRangeGroup sizeof={} alignof={}",
        size_of::<xmlChRangeGroup>(),
        align_of::<xmlChRangeGroup>()
    );
    {
        unsafe {
            let layout = Layout::new::<xmlChRangeGroup>();
            let p = alloc_zeroed(layout) as *mut xmlChRangeGroup;
            println!(
                "FIELD xmlChRangeGroup.nbShortRange offsetof={} size={}",
                offset_of!(xmlChRangeGroup, nbShortRange),
                size_of_val(&(*p).nbShortRange)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<xmlChRangeGroup>();
            let p = alloc_zeroed(layout) as *mut xmlChRangeGroup;
            println!(
                "FIELD xmlChRangeGroup.nbLongRange offsetof={} size={}",
                offset_of!(xmlChRangeGroup, nbLongRange),
                size_of_val(&(*p).nbLongRange)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<xmlChRangeGroup>();
            let p = alloc_zeroed(layout) as *mut xmlChRangeGroup;
            println!(
                "FIELD xmlChRangeGroup.shortRange offsetof={} size={}",
                offset_of!(xmlChRangeGroup, shortRange),
                size_of_val(&(*p).shortRange)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<xmlChRangeGroup>();
            let p = alloc_zeroed(layout) as *mut xmlChRangeGroup;
            println!(
                "FIELD xmlChRangeGroup.longRange offsetof={} size={}",
                offset_of!(xmlChRangeGroup, longRange),
                size_of_val(&(*p).longRange)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "STRUCT xmlChSRange sizeof={} alignof={}",
        size_of::<xmlChSRange>(),
        align_of::<xmlChSRange>()
    );
    {
        unsafe {
            let layout = Layout::new::<xmlChSRange>();
            let p = alloc_zeroed(layout) as *mut xmlChSRange;
            println!(
                "FIELD xmlChSRange.low offsetof={} size={}",
                offset_of!(xmlChSRange, low),
                size_of_val(&(*p).low)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    {
        unsafe {
            let layout = Layout::new::<xmlChSRange>();
            let p = alloc_zeroed(layout) as *mut xmlChSRange;
            println!(
                "FIELD xmlChSRange.high offsetof={} size={}",
                offset_of!(xmlChSRange, high),
                size_of_val(&(*p).high)
            );
            dealloc(p as *mut u8, layout);
        }
    }
    println!(
        "ENUM xmlAttributeDefault sizeof={} alignof={}",
        size_of::<xmlAttributeDefault>(),
        align_of::<xmlAttributeDefault>()
    );
    println!(
        "EVAL xmlAttributeDefault.XML_ATTRIBUTE_NONE={}",
        xmlAttributeDefault::XML_ATTRIBUTE_NONE as isize
    );
    println!(
        "EVAL xmlAttributeDefault.XML_ATTRIBUTE_REQUIRED={}",
        xmlAttributeDefault::XML_ATTRIBUTE_REQUIRED as isize
    );
    println!(
        "EVAL xmlAttributeDefault.XML_ATTRIBUTE_IMPLIED={}",
        xmlAttributeDefault::XML_ATTRIBUTE_IMPLIED as isize
    );
    println!(
        "EVAL xmlAttributeDefault.XML_ATTRIBUTE_FIXED={}",
        xmlAttributeDefault::XML_ATTRIBUTE_FIXED as isize
    );
    println!(
        "ENUM xmlAttributeType sizeof={} alignof={}",
        size_of::<xmlAttributeType>(),
        align_of::<xmlAttributeType>()
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_CDATA={}",
        xmlAttributeType::XML_ATTRIBUTE_CDATA as isize
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_ID={}",
        xmlAttributeType::XML_ATTRIBUTE_ID as isize
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_IDREF={}",
        xmlAttributeType::XML_ATTRIBUTE_IDREF as isize
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_IDREFS={}",
        xmlAttributeType::XML_ATTRIBUTE_IDREFS as isize
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_ENTITY={}",
        xmlAttributeType::XML_ATTRIBUTE_ENTITY as isize
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_ENTITIES={}",
        xmlAttributeType::XML_ATTRIBUTE_ENTITIES as isize
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_NMTOKEN={}",
        xmlAttributeType::XML_ATTRIBUTE_NMTOKEN as isize
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_NMTOKENS={}",
        xmlAttributeType::XML_ATTRIBUTE_NMTOKENS as isize
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_ENUMERATION={}",
        xmlAttributeType::XML_ATTRIBUTE_ENUMERATION as isize
    );
    println!(
        "EVAL xmlAttributeType.XML_ATTRIBUTE_NOTATION={}",
        xmlAttributeType::XML_ATTRIBUTE_NOTATION as isize
    );
    println!(
        "ENUM xmlBufferAllocationScheme sizeof={} alignof={}",
        size_of::<xmlBufferAllocationScheme>(),
        align_of::<xmlBufferAllocationScheme>()
    );
    println!(
        "EVAL xmlBufferAllocationScheme.XML_BUFFER_ALLOC_DOUBLEIT={}",
        xmlBufferAllocationScheme::XML_BUFFER_ALLOC_DOUBLEIT as isize
    );
    println!(
        "EVAL xmlBufferAllocationScheme.XML_BUFFER_ALLOC_EXACT={}",
        xmlBufferAllocationScheme::XML_BUFFER_ALLOC_EXACT as isize
    );
    println!(
        "EVAL xmlBufferAllocationScheme.XML_BUFFER_ALLOC_IMMUTABLE={}",
        xmlBufferAllocationScheme::XML_BUFFER_ALLOC_IMMUTABLE as isize
    );
    println!(
        "EVAL xmlBufferAllocationScheme.XML_BUFFER_ALLOC_IO={}",
        xmlBufferAllocationScheme::XML_BUFFER_ALLOC_IO as isize
    );
    println!(
        "EVAL xmlBufferAllocationScheme.XML_BUFFER_ALLOC_HYBRID={}",
        xmlBufferAllocationScheme::XML_BUFFER_ALLOC_HYBRID as isize
    );
    println!(
        "EVAL xmlBufferAllocationScheme.XML_BUFFER_ALLOC_BOUNDED={}",
        xmlBufferAllocationScheme::XML_BUFFER_ALLOC_BOUNDED as isize
    );
    println!(
        "ENUM xmlCharEncoding sizeof={} alignof={}",
        size_of::<xmlCharEncoding>(),
        align_of::<xmlCharEncoding>()
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_ERROR={}",
        xmlCharEncoding::XML_CHAR_ENCODING_ERROR as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_NONE={}",
        xmlCharEncoding::XML_CHAR_ENCODING_NONE as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_UTF8={}",
        xmlCharEncoding::XML_CHAR_ENCODING_UTF8 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_UTF16LE={}",
        xmlCharEncoding::XML_CHAR_ENCODING_UTF16LE as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_UTF16BE={}",
        xmlCharEncoding::XML_CHAR_ENCODING_UTF16BE as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_UCS4LE={}",
        xmlCharEncoding::XML_CHAR_ENCODING_UCS4LE as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_UCS4BE={}",
        xmlCharEncoding::XML_CHAR_ENCODING_UCS4BE as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_EBCDIC={}",
        xmlCharEncoding::XML_CHAR_ENCODING_EBCDIC as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_UCS4_2143={}",
        xmlCharEncoding::XML_CHAR_ENCODING_UCS4_2143 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_UCS4_3412={}",
        xmlCharEncoding::XML_CHAR_ENCODING_UCS4_3412 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_UCS2={}",
        xmlCharEncoding::XML_CHAR_ENCODING_UCS2 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_8859_1={}",
        xmlCharEncoding::XML_CHAR_ENCODING_8859_1 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_8859_2={}",
        xmlCharEncoding::XML_CHAR_ENCODING_8859_2 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_8859_3={}",
        xmlCharEncoding::XML_CHAR_ENCODING_8859_3 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_8859_4={}",
        xmlCharEncoding::XML_CHAR_ENCODING_8859_4 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_8859_5={}",
        xmlCharEncoding::XML_CHAR_ENCODING_8859_5 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_8859_6={}",
        xmlCharEncoding::XML_CHAR_ENCODING_8859_6 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_8859_7={}",
        xmlCharEncoding::XML_CHAR_ENCODING_8859_7 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_8859_8={}",
        xmlCharEncoding::XML_CHAR_ENCODING_8859_8 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_8859_9={}",
        xmlCharEncoding::XML_CHAR_ENCODING_8859_9 as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_2022_JP={}",
        xmlCharEncoding::XML_CHAR_ENCODING_2022_JP as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_SHIFT_JIS={}",
        xmlCharEncoding::XML_CHAR_ENCODING_SHIFT_JIS as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_EUC_JP={}",
        xmlCharEncoding::XML_CHAR_ENCODING_EUC_JP as isize
    );
    println!(
        "EVAL xmlCharEncoding.XML_CHAR_ENCODING_ASCII={}",
        xmlCharEncoding::XML_CHAR_ENCODING_ASCII as isize
    );
    println!(
        "ENUM xmlDocProperties sizeof={} alignof={}",
        size_of::<xmlDocProperties>(),
        align_of::<xmlDocProperties>()
    );
    println!(
        "EVAL xmlDocProperties.XML_DOC_WELLFORMED={}",
        xmlDocProperties::XML_DOC_WELLFORMED as isize
    );
    println!(
        "EVAL xmlDocProperties.XML_DOC_NSVALID={}",
        xmlDocProperties::XML_DOC_NSVALID as isize
    );
    println!(
        "EVAL xmlDocProperties.XML_DOC_OLD10={}",
        xmlDocProperties::XML_DOC_OLD10 as isize
    );
    println!(
        "EVAL xmlDocProperties.XML_DOC_DTDVALID={}",
        xmlDocProperties::XML_DOC_DTDVALID as isize
    );
    println!(
        "EVAL xmlDocProperties.XML_DOC_XINCLUDE={}",
        xmlDocProperties::XML_DOC_XINCLUDE as isize
    );
    println!(
        "EVAL xmlDocProperties.XML_DOC_USERBUILT={}",
        xmlDocProperties::XML_DOC_USERBUILT as isize
    );
    println!(
        "EVAL xmlDocProperties.XML_DOC_INTERNAL={}",
        xmlDocProperties::XML_DOC_INTERNAL as isize
    );
    println!(
        "EVAL xmlDocProperties.XML_DOC_HTML={}",
        xmlDocProperties::XML_DOC_HTML as isize
    );
    println!(
        "ENUM xmlElementContentOccur sizeof={} alignof={}",
        size_of::<xmlElementContentOccur>(),
        align_of::<xmlElementContentOccur>()
    );
    println!(
        "EVAL xmlElementContentOccur.XML_ELEMENT_CONTENT_ONCE={}",
        xmlElementContentOccur::XML_ELEMENT_CONTENT_ONCE as isize
    );
    println!(
        "EVAL xmlElementContentOccur.XML_ELEMENT_CONTENT_OPT={}",
        xmlElementContentOccur::XML_ELEMENT_CONTENT_OPT as isize
    );
    println!(
        "EVAL xmlElementContentOccur.XML_ELEMENT_CONTENT_MULT={}",
        xmlElementContentOccur::XML_ELEMENT_CONTENT_MULT as isize
    );
    println!(
        "EVAL xmlElementContentOccur.XML_ELEMENT_CONTENT_PLUS={}",
        xmlElementContentOccur::XML_ELEMENT_CONTENT_PLUS as isize
    );
    println!(
        "ENUM xmlElementContentType sizeof={} alignof={}",
        size_of::<xmlElementContentType>(),
        align_of::<xmlElementContentType>()
    );
    println!(
        "EVAL xmlElementContentType.XML_ELEMENT_CONTENT_PCDATA={}",
        xmlElementContentType::XML_ELEMENT_CONTENT_PCDATA as isize
    );
    println!(
        "EVAL xmlElementContentType.XML_ELEMENT_CONTENT_ELEMENT={}",
        xmlElementContentType::XML_ELEMENT_CONTENT_ELEMENT as isize
    );
    println!(
        "EVAL xmlElementContentType.XML_ELEMENT_CONTENT_SEQ={}",
        xmlElementContentType::XML_ELEMENT_CONTENT_SEQ as isize
    );
    println!(
        "EVAL xmlElementContentType.XML_ELEMENT_CONTENT_OR={}",
        xmlElementContentType::XML_ELEMENT_CONTENT_OR as isize
    );
    println!(
        "ENUM xmlElementType sizeof={} alignof={}",
        size_of::<xmlElementType>(),
        align_of::<xmlElementType>()
    );
    println!(
        "EVAL xmlElementType.XML_ELEMENT_NODE={}",
        xmlElementType::XML_ELEMENT_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_ATTRIBUTE_NODE={}",
        xmlElementType::XML_ATTRIBUTE_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_TEXT_NODE={}",
        xmlElementType::XML_TEXT_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_CDATA_SECTION_NODE={}",
        xmlElementType::XML_CDATA_SECTION_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_ENTITY_REF_NODE={}",
        xmlElementType::XML_ENTITY_REF_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_ENTITY_NODE={}",
        xmlElementType::XML_ENTITY_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_PI_NODE={}",
        xmlElementType::XML_PI_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_COMMENT_NODE={}",
        xmlElementType::XML_COMMENT_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_DOCUMENT_NODE={}",
        xmlElementType::XML_DOCUMENT_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_DOCUMENT_TYPE_NODE={}",
        xmlElementType::XML_DOCUMENT_TYPE_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_DOCUMENT_FRAG_NODE={}",
        xmlElementType::XML_DOCUMENT_FRAG_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_NOTATION_NODE={}",
        xmlElementType::XML_NOTATION_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_HTML_DOCUMENT_NODE={}",
        xmlElementType::XML_HTML_DOCUMENT_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_DTD_NODE={}",
        xmlElementType::XML_DTD_NODE as isize
    );
    println!(
        "EVAL xmlElementType.XML_ELEMENT_DECL={}",
        xmlElementType::XML_ELEMENT_DECL as isize
    );
    println!(
        "EVAL xmlElementType.XML_ATTRIBUTE_DECL={}",
        xmlElementType::XML_ATTRIBUTE_DECL as isize
    );
    println!(
        "EVAL xmlElementType.XML_ENTITY_DECL={}",
        xmlElementType::XML_ENTITY_DECL as isize
    );
    println!(
        "EVAL xmlElementType.XML_NAMESPACE_DECL={}",
        xmlElementType::XML_NAMESPACE_DECL as isize
    );
    println!(
        "EVAL xmlElementType.XML_XINCLUDE_START={}",
        xmlElementType::XML_XINCLUDE_START as isize
    );
    println!(
        "EVAL xmlElementType.XML_XINCLUDE_END={}",
        xmlElementType::XML_XINCLUDE_END as isize
    );
    println!(
        "ENUM xmlElementTypeVal sizeof={} alignof={}",
        size_of::<xmlElementTypeVal>(),
        align_of::<xmlElementTypeVal>()
    );
    println!(
        "EVAL xmlElementTypeVal.XML_ELEMENT_TYPE_UNDEFINED={}",
        xmlElementTypeVal::XML_ELEMENT_TYPE_UNDEFINED as isize
    );
    println!(
        "EVAL xmlElementTypeVal.XML_ELEMENT_TYPE_EMPTY={}",
        xmlElementTypeVal::XML_ELEMENT_TYPE_EMPTY as isize
    );
    println!(
        "EVAL xmlElementTypeVal.XML_ELEMENT_TYPE_ANY={}",
        xmlElementTypeVal::XML_ELEMENT_TYPE_ANY as isize
    );
    println!(
        "EVAL xmlElementTypeVal.XML_ELEMENT_TYPE_MIXED={}",
        xmlElementTypeVal::XML_ELEMENT_TYPE_MIXED as isize
    );
    println!(
        "EVAL xmlElementTypeVal.XML_ELEMENT_TYPE_ELEMENT={}",
        xmlElementTypeVal::XML_ELEMENT_TYPE_ELEMENT as isize
    );
    println!(
        "ENUM xmlEntityType sizeof={} alignof={}",
        size_of::<xmlEntityType>(),
        align_of::<xmlEntityType>()
    );
    println!(
        "EVAL xmlEntityType.XML_INTERNAL_GENERAL_ENTITY={}",
        xmlEntityType::XML_INTERNAL_GENERAL_ENTITY as isize
    );
    println!(
        "EVAL xmlEntityType.XML_EXTERNAL_GENERAL_PARSED_ENTITY={}",
        xmlEntityType::XML_EXTERNAL_GENERAL_PARSED_ENTITY as isize
    );
    println!(
        "EVAL xmlEntityType.XML_EXTERNAL_GENERAL_UNPARSED_ENTITY={}",
        xmlEntityType::XML_EXTERNAL_GENERAL_UNPARSED_ENTITY as isize
    );
    println!(
        "EVAL xmlEntityType.XML_INTERNAL_PARAMETER_ENTITY={}",
        xmlEntityType::XML_INTERNAL_PARAMETER_ENTITY as isize
    );
    println!(
        "EVAL xmlEntityType.XML_EXTERNAL_PARAMETER_ENTITY={}",
        xmlEntityType::XML_EXTERNAL_PARAMETER_ENTITY as isize
    );
    println!(
        "EVAL xmlEntityType.XML_INTERNAL_PREDEFINED_ENTITY={}",
        xmlEntityType::XML_INTERNAL_PREDEFINED_ENTITY as isize
    );
    println!(
        "ENUM xmlErrorLevel sizeof={} alignof={}",
        size_of::<xmlErrorLevel>(),
        align_of::<xmlErrorLevel>()
    );
    println!(
        "EVAL xmlErrorLevel.XML_ERR_NONE={}",
        xmlErrorLevel::XML_ERR_NONE as isize
    );
    println!(
        "EVAL xmlErrorLevel.XML_ERR_WARNING={}",
        xmlErrorLevel::XML_ERR_WARNING as isize
    );
    println!(
        "EVAL xmlErrorLevel.XML_ERR_ERROR={}",
        xmlErrorLevel::XML_ERR_ERROR as isize
    );
    println!(
        "EVAL xmlErrorLevel.XML_ERR_FATAL={}",
        xmlErrorLevel::XML_ERR_FATAL as isize
    );
    println!(
        "ENUM xmlParserInputState sizeof={} alignof={}",
        size_of::<xmlParserInputState>(),
        align_of::<xmlParserInputState>()
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_EOF={}",
        xmlParserInputState::XML_PARSER_EOF as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_START={}",
        xmlParserInputState::XML_PARSER_START as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_MISC={}",
        xmlParserInputState::XML_PARSER_MISC as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_PI={}",
        xmlParserInputState::XML_PARSER_PI as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_DTD={}",
        xmlParserInputState::XML_PARSER_DTD as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_PROLOG={}",
        xmlParserInputState::XML_PARSER_PROLOG as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_COMMENT={}",
        xmlParserInputState::XML_PARSER_COMMENT as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_START_TAG={}",
        xmlParserInputState::XML_PARSER_START_TAG as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_CONTENT={}",
        xmlParserInputState::XML_PARSER_CONTENT as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_CDATA_SECTION={}",
        xmlParserInputState::XML_PARSER_CDATA_SECTION as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_END_TAG={}",
        xmlParserInputState::XML_PARSER_END_TAG as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_ENTITY_DECL={}",
        xmlParserInputState::XML_PARSER_ENTITY_DECL as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_ENTITY_VALUE={}",
        xmlParserInputState::XML_PARSER_ENTITY_VALUE as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_ATTRIBUTE_VALUE={}",
        xmlParserInputState::XML_PARSER_ATTRIBUTE_VALUE as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_SYSTEM_LITERAL={}",
        xmlParserInputState::XML_PARSER_SYSTEM_LITERAL as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_EPILOG={}",
        xmlParserInputState::XML_PARSER_EPILOG as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_IGNORE={}",
        xmlParserInputState::XML_PARSER_IGNORE as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_PUBLIC_LITERAL={}",
        xmlParserInputState::XML_PARSER_PUBLIC_LITERAL as isize
    );
    println!(
        "EVAL xmlParserInputState.XML_PARSER_XML_DECL={}",
        xmlParserInputState::XML_PARSER_XML_DECL as isize
    );
    println!(
        "ENUM xmlParserMode sizeof={} alignof={}",
        size_of::<xmlParserMode>(),
        align_of::<xmlParserMode>()
    );
    println!(
        "EVAL xmlParserMode.XML_PARSE_UNKNOWN={}",
        xmlParserMode::XML_PARSE_UNKNOWN as isize
    );
    println!(
        "EVAL xmlParserMode.XML_PARSE_DOM={}",
        xmlParserMode::XML_PARSE_DOM as isize
    );
    println!(
        "EVAL xmlParserMode.XML_PARSE_SAX={}",
        xmlParserMode::XML_PARSE_SAX as isize
    );
    println!(
        "EVAL xmlParserMode.XML_PARSE_PUSH_DOM={}",
        xmlParserMode::XML_PARSE_PUSH_DOM as isize
    );
    println!(
        "EVAL xmlParserMode.XML_PARSE_PUSH_SAX={}",
        xmlParserMode::XML_PARSE_PUSH_SAX as isize
    );
    println!(
        "EVAL xmlParserMode.XML_PARSE_READER={}",
        xmlParserMode::XML_PARSE_READER as isize
    );
    println!(
        "ENUM xmlXPathObjectType sizeof={} alignof={}",
        size_of::<xmlXPathObjectType>(),
        align_of::<xmlXPathObjectType>()
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_UNDEFINED={}",
        xmlXPathObjectType::XPATH_UNDEFINED as isize
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_NODESET={}",
        xmlXPathObjectType::XPATH_NODESET as isize
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_BOOLEAN={}",
        xmlXPathObjectType::XPATH_BOOLEAN as isize
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_NUMBER={}",
        xmlXPathObjectType::XPATH_NUMBER as isize
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_STRING={}",
        xmlXPathObjectType::XPATH_STRING as isize
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_POINT={}",
        xmlXPathObjectType::XPATH_POINT as isize
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_RANGE={}",
        xmlXPathObjectType::XPATH_RANGE as isize
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_LOCATIONSET={}",
        xmlXPathObjectType::XPATH_LOCATIONSET as isize
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_USERS={}",
        xmlXPathObjectType::XPATH_USERS as isize
    );
    println!(
        "EVAL xmlXPathObjectType.XPATH_XSLT_TREE={}",
        xmlXPathObjectType::XPATH_XSLT_TREE as isize
    );
}
