{
  "case_id": "CLI-XMLLINT-0001",
  "surface": "cli-xmllint",
  "purpose": "xmllint --version produces expected output format and exit status",
  "oracle_profile": "libxml2-2.15.3",
  "inputs": {
    "argv": ["--version"]
  },
  "observables": [
    "exit_status",
    "stdout_hash",
    "stderr_hash"
  ],
  "normalization": [],
  "provenance": [
    "upstream-documentation"
  ]
}
