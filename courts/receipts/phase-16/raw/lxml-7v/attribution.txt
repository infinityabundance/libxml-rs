Phase 16.7.8 step 7v — lxml consumer court: push blocker fixed, schematron
crash fixed, and the remaining suite failures measured against a baseline.

WHY THIS RECEIPT EXISTS
-----------------------
The step-7v option flip makes lxml's default parse-option set
(NOENT | NOCDATA | NONET | COMPACT | BIG_LINES) eligible for the persistent
push driver. Before it every lxml parse stayed on the O(N^2) replay engine and
lxml's 2.2 GB test_very_large_sourceline_iterparse hung for more than 240 s.
After it the suite runs past that test (20.5 s in the debug build the court
uses; ~2.4 GB/s in release) and can finally be measured end to end.

PART 1 — A REAL BUG FOUND BY THE UNBLOCKED SUITE (FIXED)
-------------------------------------------------------
The suite aborted rc=139 at the `lxml.etree.Schematron` doctest:
`xmlSchematronNewValidCtxt` cloned a `CompiledExpr` from freed memory.

Root cause: `xmlSchematronParse` returned its PARSER CONTEXT as the schema
pointer. Upstream keeps `_xmlSchematronParserCtxt` and `_xmlSchematron` as two
objects; lxml does

    self._c_schema = xmlSchematronParse(parser_ctxt)
    finally: xmlSchematronFreeParserCtxt(parser_ctxt)

and frees the schema only at dealloc, so the schema pointer was dangling
(and `xmllint --schematron` double-freed). Fix: a distinct
`SchematronParserCtxt` type and a NEW `Box<SchematronSchema>` from
`xmlSchematronParse`; `xmlSchematronFreeParserCtxt` frees only the parser
context.

Verified against the oracle with a direct probe (no lxml):

    candidate  validate -> False   rc=0
    oracle     validate -> False   rc=0

New regression tests: `parser_ctxt_and_schema_are_distinct_allocations`,
`parse_twice_recompiles_independently`; the existing C-ABI lifecycle tests were
corrected to the upstream sequence.

PART 2 — THE REMAINING FAILURES ARE PRE-EXISTING (MEASURED)
-----------------------------------------------------------
Full suite via the court's own runner (`python3 test.py -u -v`):

  baseline  = 3490fe47 + ONLY the schematron fix above, debug, in-container
              (test_very_large_sourceline_iterparse deselected: it HANGS on the
              pre-7v replay engine, which is the blocker this slice fixes)
  candidate = this slice, debug

    baseline    Ran 2007 tests   FAILED (failures=114, errors=59)   => 173
    candidate   Ran 2007 tests   FAILED (failures=104, errors=59)   => 163

The slice therefore regresses NOTHING and fixes 10 tests (the flip moves some
feed-parser / iterparse cases off the replay engine). The residual 163 failures
are pre-existing and are NOT push-parser defects; by module:

    21 ERROR lxml.tests.test_isoschematron     10 FAIL lxml.tests.test_xmlschema
    19 FAIL  lxml.tests.test_htmlparser        10 FAIL lxml.tests.test_xslt (+6 ERROR)
    13 FAIL  lxml.tests.test_etree (+10 ERR)    9 FAIL lxml.tests.test_xpathevaluator
     6 FAIL  lxml.tests.test_dtd (+5 ERROR)     6 FAIL lxml.tests.test_relaxng
     6 FAIL  lxml.tests.test_incremental_xmlfile (+4 ERROR)
     4 ERROR lxml.tests.test_http_io            4 ERROR lxml.tests.test_objectify
     4 FAIL  lxml.tests.test_elementpath        13 FAIL doc/*.txt doctests, etc.

ROOT CAUSE FOUND FOR THE LARGEST SINGLE CLUSTER (isoschematron, 21 + a doctest)
------------------------------------------------------------------------------
`iso_dsdl_include.xsl` compiles, but its generic `node()` copy template never
runs: the process outputs only the two `DSDL_INCLUDE_*` PIs. Bisection (drop one
specific template at a time) isolates it to this template:

    <xslt:template match="*[@xlink:href][not(parent::*[@xlink:type='complex'])]
        [not(@xlink:type) or (@xlink:type='simple')][@xlink:show='embed']
        [not(@xlink:actuate) or (@xlink:actuate='onLoad')]" mode="dsdl:go">

With that template removed the transform copies the schema correctly
(outlen 403, root=schema) — the candidate then matches the oracle. With it
present the generic `node()` template (priority -1) is shadowed and the XLink
body runs for the schema root, emitting the PIs. This is an XSLT match-PATTERN
compilation defect (predicates on a wildcard step are apparently not applied),
not a push-parser issue; a minimal `mode`/prefix repro does NOT reproduce it, so
it is specifically the multi-predicate wildcard match.

CONCLUSION
----------
* lxml 2.2 GB hang — FIXED (push parser; the §16.7.8 blocker).
* schematron segfault — FIXED (real use-after-free, oracle-verified).
* lxml suite failures 173 -> 163, no regressions — measured against a baseline.
* The remaining 163 are a separate XSLT / HTML / XML-Schema / XPath parity
  program; the isoschematron cluster's root cause is documented above.

PART 3 — THREE XSLT BUGS FIXED FROM THAT ROOT CAUSE (commit 5785ccd)
--------------------------------------------------------------------
Chasing the isoschematron cluster found three real, oracle-verified defects:

1. Template selection passed a NULL transform context to `xsltTestPattern`, and
   `match_step` treats predicates as satisfied without a context — so every
   predicate-bearing `match` pattern matched EVERY node.
2. `evaluate_predicates` set the XPath context to the PREVIOUS context node
   (`saved_node`) instead of the node under test.
3. `copy_node_deep` had no attribute arm, so `xsl:copy-of select="@*"` dropped
   every attribute.

Result on the isolated oracle probes (direct libxslt, no lxml):
  `iso_dsdl_include.xsl` -> candidate NOW BYTE-IDENTICAL to the oracle
                            (513 bytes with attributes; was 66 bytes of PIs)
  minimal `match="*[@hit]"` repro -> identical to the oracle
Regression tests added; `cargo test --lib` 1322 passed; PHP six-gate
unchanged (1250 passed / 0 failed).

NEWLY EXPOSED (NOT introduced, but now reachable) — the next blocker:
With predicates honored and attributes copied, the isoschematron pipeline
reaches `iso_abstract_expand.xsl`'s real recursive macro expansion.
Isolated with the same probe:

    oracle    iso_abstract_expand.xsl + abstract schematron  -> 632 bytes, instant
    candidate iso_abstract_expand.xsl + abstract schematron  -> does not finish in 20 s

So `lxml.tests.test_isoschematron` now HANGS (it previously errored out early
because templates matched the wrong nodes). The engine spends unbounded time
in one `xsl:for-each` / `xsl:apply-templates` cycle at shallow stack depth
(gdb shows a ~20-frame stack with repeated allocation inside
`process_apply_templates` -> `xsltPushVariable`), i.e. a node-set iteration /
macro-expansion non-termination in the transform engine — a separate,
pre-existing scalability defect. Reproducer retained:
`/tmp/it/xslt/iso_abstract_expand.xsl` + `/tmp/it/xslt/abs.xml` (see sch.py).

NOTE: always verify LD_LIBRARY_PATH with ldd. A directory that does not exist
inside the container silently falls back to /usr/local/lib — the ORACLE — which
invalidated an earlier "pristine passes" comparison in this session.
