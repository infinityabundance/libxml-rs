# residual-exports.txt — header-compile court (11.1-S) explicit open residuals
#
# Functions that the drop-in headers DECLARE (upstream contract, verified by
# the 11.1-S header-surface audit against /usr/include 2.15.3/1.1.45/0.8.25)
# that the candidate DSO does not yet export. The header-compile court
# check #5 (declared-functions-exported) treats every symbol in this file as
# an expected open residual instead of a failure.
#
# EMPTY as of 11.1-X: every declared symbol (including the xlink surface,
# the xmlCtxt/xmlNewInputFrom* families, the schematron/xslt/exslt helpers
# and the version data symbols) is exported by the candidate DSO, so the
# check #5 runs unrelaxed. R-000165 closed.
#
# Phase 12 (EXPORT-SURFACE-DISPOSITION): the shipped shared-DSO surface is
# the exact executed-oracle surface (plus the documented extension plane),
# and the executed oracle DSOs THEMSELVES do not export these four
# symbols — they are declared in the upstream headers only (the oracle's own
# headers declare them; its DSOs do not). The candidate now matches the
# oracle: hidden from the shared DSOs by the generated version scripts
# (INTERNAL_LEAK disposition; still linkable through the staticlib). These
# are the R-000165-class header-declared/DSO-absent residuals for the
# Phase-12 contract.
xsltStyleStylesheetLevelGetExtData
xsltGetUTF8CharZ
xsltSetCtxtLocaleHandlers
exsltCryptoRegister
