# Rust build artifacts
target/

# Cargo-fuzz is a detached workspace (publish=false); its lockfile is a
# generated local artifact, not part of the reproducible published build.
fuzz/Cargo.lock

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Editor/IDE files
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/
*.iml

# OS files
Thumbs.db
.directory

# Scratch investigation artifacts (Phase 15 courts) — not part of source
/evfeed.py
/feed6.c
/micro.c
/push_probe.c
/xsltrepro.py

# Docker build artifacts
docker/build/
docker/*.tar.xz

# Oracle build artifacts
oracle/build/
oracle/*.tar.xz

# Historical oracle matrix (§41-42, §51) — regenerable build/capture artifacts.
# Rebuild with: oracle/historical/build.sh; re-capture with: oracle/historical/run_matrix.sh
oracle/historical/src/
oracle/historical/prefix/
oracle/historical/results/
oracle/historical/styles/

# Doxygen archaeology (11.1-B/C) — regenerable internals; the normalized census
# (census.json, *-surface-epochs.json, conditions/coverage/configs.json) is committed.
oracle/historical/doxygen/*/src/
oracle/historical/doxygen/*/xml-public/
oracle/historical/doxygen/*/xml-full/
oracle/historical/doxygen/*/archive.tar
oracle/historical/doxygen/*/Doxyfile-*
oracle/historical/doxygen/*/profile-*.json
oracle/historical/doxygen/.probe-*

# Receipts (generated during court runs)
receipts/*.json

# Large generated data (these are committed as archaeological records)
# atlas/api/**/*.json
# atlas/abi/**/*.json
# atlas/symbols/**/*.json

# Spec file - not part of the published crate
libxml-rs-prompt.txt

# Archaeology - upstream git clones (local development only)
# These are large git repositories that should be cloned separately.
# See atlas/SOURCES.md for clone instructions.
archaeology/libxml2-git/
archaeology/libxslt-git/

# Cargo lock is committed for reproducible builds
# Cargo.lock
vgcore.*
