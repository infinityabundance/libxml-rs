//! xmllint — XML validation and formatting tool (§36, §85 Phase 10).
//!
//! Phase 0: scaffolded. CLI implementation begins in Phase 10.

fn main() {
    // Phase 0: stub
    eprintln!("xmllint: not yet implemented (Phase 0 — Archaeological Seizure)");
    std::process::exit(1);
}
